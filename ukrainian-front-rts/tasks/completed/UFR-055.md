# UFR-055 — Add deterministic construction placement

- PR: #41
- Completed commit: pending maintainer squash merge
- Acceptance: Adds whole-tile building footprints, 90-degree rotation when the tile footprint changes, deterministic snapping, explicit foundation/overlap/access failures, mud/rubble/road flattening, exact navigation blocker metadata, and a green/amber/red placement preview with local path-sever warnings.
- Verification: Exact connector-visible blobs matched local Git hashes for all seven implementation, integration, test, and documentation files. `node --check` passed for `src/systems/construction-placement-system.js`, `src/input/construction-placement-input.js`, `src/render/construction-preview.js`, `src/systems/navigation-movement-system.js`, `src/main.js`, and `tests/economy/construction-placement-system.test.mjs`. `node --test tests/economy/construction-placement-system.test.mjs` passed 10/10 tests. A focused 80×52-grid benchmark completed 200 uncached placement evaluations in approximately 131.8 ms total, about 0.66 ms per evaluation; repeated same-tile previews are cached by placement and navigation state.
- Verification limitations: Native full-repository `bash verify.sh`, `./run.sh`, and browser mission playtesting were unavailable because the execution environment could not obtain the repository outside the GitHub connector. Browser checks are documented in `docs/CONSTRUCTION.md`.
- Notes: Current `main` through `a92c54faf42ff0deadd339ec07b050b5487ecdcb`, including UFR-035 and UFR-036, was reviewed and has no file overlap. Scope remains placement validation and preview only; multiple builders, cancellation/refunds, construction lifecycle, production exits, and fortification content remain later tasks.
