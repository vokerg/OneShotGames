# UFR-004 — Introduce seeded simulation randomness

- PR: #13
- Completed commit: pending maintainer squash merge
- Verification: `node --check` passed for every changed JavaScript module and verifier; `bash -n verify.sh` passed; `node scripts/verify-task-queue.test.mjs` reported 7 passing cases; `node scripts/verify-task-queue.mjs TASKS.md` passed for 168 tasks; `node scripts/verify-content-schema.mjs` passed for 8 families; `node scripts/verify-seeded-random.mjs` passed same-seed placement, wave, combat, mission-reset, and snapshot/restore checks; the complete verification command passed in a reconstructed local fixture using exact connector-fetched changed files and prerequisite verifier files. Connector inspection confirmed the unchanged production `Game` routes hero placement, initial cooldowns, and production exit placement through `randomBetween`, contains no direct `Math.random`, and all production mission launch/retry paths use `runtime.startMission`. A complete repository checkout and browser session are not exposed in the connector environment, so the full real-checkout `bash verify.sh` and manual mission/control playtest could not be executed.
- Notes: Adds a dependency-free seeded random service with stable string/number normalization, mission-seed derivation, range/integer/pick APIs, and snapshot/restore. `randomBetween` now consumes the authoritative mission stream; runtime startup resets and records `game.simulationSeed`; wave deployment receives small seeded positional jitter; projectile impacts use a seeded ±5% damage roll. Direct simulation `Math.random` calls are rejected by verification. No content-schema version change, new content IDs, asset, audio, or save format is introduced; future save/replay/checkpoint work must serialize the random snapshot.
