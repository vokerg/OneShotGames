# UFR-020 — Integrate path requests and waypoint following

- PR: #31
- Completed commit: e1cf3eef2045bc7fffa2e13ae39ca27f361d44d6
- Acceptance: Creates the active navigation grid from runtime terrain data, registers live building footprints as deterministic dynamic blockers, routes ground move and attack-move orders through bounded A*, follows world-space waypoints through the existing fixed-step `Game.updateUnit()` contract, invalidates routes when structures change, preserves direct air-unit movement, and safely cancels blocked or unreachable orders with player feedback.
- Verification: `node --check src/navigation/waypoint-route.js`; `node --check src/systems/navigation-movement-system.js`; `node --check src/systems/simulation-phases.js`; syntax checks for all four navigation test files; `node --test tests/navigation/*.test.mjs` in a reconstructed focused fixture using the exact navigation/system modules and the relevant connector-fetched config values (28 tests passed, 0 failed). A 150-ground-unit route-assignment benchmark completed in 124.064 ms total, 0.827 ms/route average, with 150 routes found.
- Verification limitations: The connector environment exposes no repository checkout and the local container cannot resolve `github.com`, so native `bash verify.sh`, `./run.sh`, and browser mission playtesting could not be performed. Connector diff review and focused reconstruction were used instead.
- Documentation: `docs/NAVIGATION.md` records runtime terrain mapping, building blocker synchronization, revision invalidation, fixed-step waypoint ownership, air-unit compatibility, and failure handling. Collision/separation, caching, formations, stuck recovery, and additional command types remain later queue tasks.
