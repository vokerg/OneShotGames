# UFR-156 — Add runtime diagnostics and fatal-error recovery

- Pull request: #180
- Branch: `ufrts/UFR-156-runtime-diagnostics`
- Evidence level: `CONTRACT_COMPLETE`
- Verification follow-up: #181
- Merge waiver: maintainer instruction on 2026-08-06 to review, repair, and squash-merge the three active PRs despite the inactive Actions runner

## Delivered

- Added a user-visible fatal-error alert dialog with bounded technical diagnostics and copy support.
- Added application-local recovery export, application-only reset, and unrelated-storage preservation.
- Added explicit runtime-invariant error policy plus `window.error` and `unhandledrejection` capture.
- Installed diagnostics before renderer/game startup and added exact listener, view, report, and global teardown.
- Repaired the reviewed destructive recovery path so data is deleted only after export returns successfully and the player explicitly confirms that the recovery file is visible; cancellation leaves storage unchanged.

## Verification evidence

Focused tests cover invariant semantics, bounded deterministic reports, privacy exclusions, application-only storage collection/reset, canonical recovery bundles, window error/rejection capture, repeated disposal, failed-export safety, confirmation cancellation, and confirmed reset.

The required repository workflow did not execute because GitHub Actions remained queued without an active runner. No browser startup or real download-policy pass is claimed. Issue #181 owns authoritative `bash verify.sh`, browser smoke, and manual recovery-flow validation on merged `main`.

## Runtime composition evidence

The diagnostics bootstrap is imported before renderer viewport setup. It observes browser error boundaries and reads optional diagnostic globals without changing simulation ownership or lifecycle methods.

## Browser/player evidence

Real browser download, clipboard, confirmation, focus, and storage-policy behavior remains outstanding under #181. The completion record does not claim `PLAYER_VERIFIED` evidence.

## Save implications

No save schema is changed. Recovery export preserves raw application-owned local-storage values in a local JSON bundle. Confirmed reset removes only keys beginning with `fields-of-resolve:` and leaves unrelated site data untouched.

## AI-system use

Diagnostics may summarize installed modules and bounded runtime counts but does not alter AI observations, doctrine, cadence, commands, or state.

## Limitations

- Browser APIs cannot prove that an anchor-triggered file was persisted; the explicit post-export confirmation is therefore part of the safety contract.
- Completion evidence must not be raised above `CONTRACT_COMPLETE` until #181 records passing integrated and browser checks.
