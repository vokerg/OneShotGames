# UFR-084 — Add versioned campaign profile state

- PR: #46
- Completed commit: pending maintainer squash merge
- Acceptance: Adds a versioned immutable campaign profile with stable identity, difficulty, operation unlocks/completions, persistent JSON-compatible choices, mission results, upgrades, medals, monotonic revisions, canonical serialization, and corruption-safe validation.
- Verification: Exact connector-visible source and test blobs were restored from the prior released claim. `node --check src/core/campaign-profile.js`; `node --check tests/campaign/campaign-profile.test.mjs`; `node --test tests/campaign/campaign-profile.test.mjs` (8 passed, 0 failed). Coverage includes deterministic defaults, idempotent difficulty/set mutations, stable sorted unlock sets, defensive choice cloning/freezing, mission-result aggregation and victory preservation, locked-operation rejection, canonical round trips, malformed JSON, unsupported versions, and inconsistent profile rejection.
- Verification limitations: Native full-repository `bash verify.sh`, `./run.sh`, and browser testing were unavailable because the execution environment could not obtain the repository outside the GitHub connector. Browser testing is not applicable to this dependency-free state contract.
- Notes: `src/core/campaign-profile.js` owns campaign state shape and deterministic mutation only. UFR-085 retains save slots/storage/autosave/migrations, UFR-086 mission scripting, UFR-089 campaign UI, and UFR-091 progression balance. No combat, production, navigation, input, UI, renderer, audio, or sibling-game behavior is changed.
