# UFR-051 — Add explicit gather commands

- PR: #38
- Completed commit: pending maintainer squash merge
- Verification: Exact connector-visible blobs matched local Git hashes `57cf6d94c75aca34c80da2c8d7392e7a3590692a` (`src/systems/worker-gather-system.js`), `ec3c9306a778e6c049801d0851f5cf775df30b33` (`tests/economy/worker-gather-system.test.mjs`), and `a6dae04f814d31e53a98a479f5d9a914d4273c0f` (`src/main.js`). `node --check` passed for all three files, and `node --test tests/economy/worker-gather-system.test.mjs` passed 8/8 tests covering deterministic resource and drop-off selection, explicit assignment, gather/return/deposit/resume, same-kind depletion retargeting, reassignment with carried cargo, resource right-click integration, order/Stop/construction cancellation, and atomic multi-worker preflight. Current `main` integrations UFR-033 and UFR-034 were reviewed and do not overlap this worker/economy scope. Native full-repository `bash verify.sh` and browser playtesting were unavailable in the connector-only environment.
- Notes: Replaces implicit idle-worker auto-gathering in the browser composition with explicit player-issued resource assignments. Workers persist a resource type, return full loads to the nearest valid completed friendly drop-off, resume after delivery, and retarget depleted sources deterministically. HQ remains the compatibility drop-off; future UFR-053 `dropOffKinds` data is supported without adding capabilities here. Normal orders, Stop, and construction cancel gathering. No resource-rate balance, worker HUD, building schema, construction, production, combat, visibility, or sibling-game behavior changes.
