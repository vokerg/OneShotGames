# UFR-123 — Meet visual performance and memory budgets

- Final PR: #229
- Completion evidence: **RUNTIME_INTEGRATED**
- Base: `4b84ad342a50b232a3132b20ca7dd6ac3cc4d94d`

## Delivered

- renderer-side viewport culling for units, buildings, resource nodes, projectiles, and effects without mutating simulation-owned arrays;
- nearest-neighbor enforcement on battlefield/render contexts to prevent smoothing artifacts in pixel-art output;
- release-art ownership classification and procedural-fallback suppression for atlas-owned infantry, vehicles, and support entities;
- bounded lazy support-frame decoding with a 192-frame LRU limit and explicit eviction accounting, preventing the UFR-114 support atlas from growing decoded-image memory without bound;
- visual-performance status instrumentation exposing drawn/culled counts and frame-budget measurements for runtime review;
- viewport bootstrap integration so the performance/fallback guard is installed in the live renderer rather than existing only as a library contract;
- deterministic regression coverage for atlas ownership, release fallback behavior, culling boundaries, array preservation, nearest-neighbor contexts, and the bounded support-frame cache.

## Verification

Exact-head workflow `31419922964` on `516c83c7ff920d10a9c6ac037957b1d64bbb2381` passed the full assembled verifier and the repository browser/UI/art/audio/accessibility/localization/evidence gates.

Self-review found that the first implementation declared a 192-frame decoded support-cache budget but did not enforce it. The branch was corrected before closeout with bounded LRU eviction and regression coverage; the green exact-head workflow above includes that fix.

The completion level is `RUNTIME_INTEGRATED`: the performance patch and release fallback guard are mounted by the live viewport bootstrap and verified by the assembled workflow.

The closeout commit is followed by one final exact-head workflow before squash merge.
