# UFR-060 — Add technology graph schema

- PR: #18
- Completed commit: pending maintainer squash merge
- Verification: `node --check` is enforced for all new `.mjs` files by `bash verify.sh`; focused graph fixtures are wired through `node scripts/verify-tech-graph.test.mjs`; production graph validation is wired through `node scripts/verify-tech-content.mjs`; connector diff review confirms all changes remain under `ukrainian-front-rts/`. A native full-checkout `bash verify.sh` run was not available in the connector-only execution environment.
- Notes: Extends schema version 1 additively for building/upgrade prerequisites, faction restrictions, mission locks, mutually exclusive choices, roots, and mission availability overrides. Legacy scalar upgrade prerequisites remain accepted during migration. No research timing, queue behavior, stat application, UI, balance, save format, art, or audio behavior changed.
