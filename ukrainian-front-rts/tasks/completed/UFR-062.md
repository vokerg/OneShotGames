# UFR-062 — Add deterministic upgrade modifier application

- PR: #77
- Final synchronized base: `3c6b949bdcfdcfd8f14bcd4fe07cafa3586917a9`
- Implementation head before completion evidence: `4c64961a3263878ddcd5e388980fcd08b7cbeb80`
- Parallel rule: `LIMITED — stats contract owner.`
- Dependencies verified on `main`: UFR-031, UFR-060

## Delivered

- Versioned, deeply immutable upgrade definitions and active-upgrade state.
- Data-only targeting by faction, unit type, archetype, vehicle class, tags, and required abilities.
- Explicit additive-before-multiplicative numeric ordering with stable priority, upgrade-ID, and declaration-index tie-breaking.
- Deterministic entity-stat and ability-parameter resolution with per-field provenance.
- Fail-closed handling for duplicate definitions, unsupported operations, negative multipliers, missing numeric bases, unknown abilities, unknown active upgrades, class instances, and browser objects.
- Stable visual-token union and deterministic visual-variant conflict resolution with provenance.
- Identical resolved profiles for existing and newly created entities.
- Existing-entity health reconciliation policies for preserved ratio, preserved deficit, and clamped current health.
- Versioned, reference-free upgrade save snapshots with known-ID and future-version validation.
- Focused ownership and composition guidance in `docs/UPGRADE_MODIFIERS.md`.

## Verification

Focused local reconstruction of the connector-authored files:

- `node --check src/systems/upgrade-modifier-system.js` — passed.
- `node --test tests/economy/upgrade-modifier-system.test.mjs` — 12/12 passed.
- Coverage includes normalization, target filters, additive/multiplicative ordering, stable visual conflicts, ability modifiers, unknown ability rejection, new numeric stats, missing-base rejection, existing/new entity parity, all health policies, immutability, save round trips, unknown IDs, future versions, and duplicate definitions.
- Connector-visible blobs exactly matched the locally verified Git object hashes:
  - `ee0358a4983749a69ca215dfec9e4beb87b2fb6e` — modifier module
  - `1672a86eca3625a6da50d96998f1b62e433d249e` — focused tests
  - `49e86cd3b4bf5bc65a2ec4dfa1f84fe9e1355a2a` — documentation
- Connector comparison showed the implementation branch two commits ahead and zero behind current `main`, with changes confined to the isolated module, one focused test file, documentation, and the task claim before this marker.

## Verification limitations

- Native full-repository `bash verify.sh` could not be run because this environment cannot obtain a complete checkout outside the GitHub connector.
- `./run.sh` and browser mission playtesting were unavailable in the connector-only environment.
- Live `Game.unitStats()` composition was intentionally not edited while the active UFR-056 construction owner may still require the shared `Game` hotspot. The contract provides the authoritative replacement resolution and atomic existing/new-entity patches for that composition boundary.

## Notes

UFR-060 owns technology graph eligibility. UFR-061 owns research timing and completion. UFR-062 owns modifier ordering, profile resolution, ability and visual upgrade descriptors, existing/new entity parity, and upgrade save snapshots. UFR-045 veterancy should compose after the upgrade baseline. Faction, UI, renderer, campaign-save, AI, and balance owners consume this contract without duplicating its rules.
