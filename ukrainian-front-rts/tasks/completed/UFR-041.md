# UFR-041 — Add generalized ability targeting modes

- PR: #57
- Completed commit: pending maintainer squash merge
- Base synchronized with `main`: `dab3196bcdbdfa7d58523ab04147b76010fcb8aa`
- Implementation head before completion evidence: `80dee360894f6f8f4977e842b031ccb59791e56f`
- Parallel rule: LIMITED — ability contract owner
- Dependencies verified on `main`: UFR-013, UFR-031

## Delivered

- Versioned immutable contracts for point, unit, area, direction, self, toggle, and channel targeting modes.
- Deterministic idle, targeting, and channeling lifecycle with explicit confirmation and cancellation records.
- Cooldown and actor-availability validation before targeting starts.
- Exact range-boundary validation, target-footprint range handling, allegiance checks, target-domain checks, and living-target validation.
- Fail-closed passability and line-of-sight integration contracts.
- Reference-free presentation telegraphs for valid and invalid previews.
- Deterministic toggle persistence and channel completion/interruption semantics.
- Focused ownership and integration guidance in `docs/ABILITY_TARGETING.md`.

## Verification

- `node --check src/combat/ability-targeting-system.js` — passed in the focused local mirror.
- `node --test tests/combat/ability-targeting-system.test.mjs` — 10/10 passed.
- Coverage includes every targeting mode, invalid profile rejection, cooldown and availability rejection, exact point/unit range boundaries, passability, line of sight, allegiance, target domains, collision radius, direction normalization, self snapshots, toggle persistence, channel completion, interruption, and explicit cancellation.
- Connector-visible module, test, and documentation blob SHAs exactly matched the locally verified Git blob hashes.
- Connector comparison after synchronization showed the branch ahead of current `main` with zero behind commits and changes confined to four UFR-041-owned files.
- Active PR #56 changes only the separate air-defense module, test, documentation, and completion marker; no file overlap exists.
- Native full-repository `bash verify.sh`, `./run.sh`, and browser mission checks could not run because this environment cannot obtain a repository checkout outside the GitHub connector.
- Browser playtesting is not applicable to the branch behavior itself because UFR-041 defines a pure contract and deliberately does not integrate existing runtime ability effects, input, UI, or rendering.

## Notes

Existing ability effects, cooldown storage, command wiring, UI, and rendering remain in their current owners. Later integration work must consume this contract rather than duplicate target validation or telegraph rules.
