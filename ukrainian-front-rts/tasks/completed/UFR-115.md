# UFR-115 — Produce complete faction building atlas

- Pull request: #139
- Branch: `ufrts/UFR-115-building-atlas`
- Original base: `860a18ba5fbbedd27cea746c0f4efc60a8326500`
- Synchronized implementation head: `31839a8a939ca2eab8b9ef1f0e9f13a4ff891d41`
- Evidence level: `CONTRACT_COMPLETE`

## Delivered

- Added a versioned, machine-readable, repository-authored CC0 source contract for 16 canonical production structures: eight Ukrainian and eight Russian variants covering command, logistics, infantry, vehicle, UAS/EW, fires, air-defense, and engineer roles.
- Added deterministic in-memory SVG atlas generation for placement, foundation, frame, fitout, idle, active, damaged, critical, three destruction frames, rubble, and build-menu icon states.
- Produced 208 production frames, 210 runtime frames including two diagnostic fallbacks, and 176 stable lifecycle animations.
- Defined 96×96 battlefield canvases, 40×40 icons, stable anchors, footprint and selection masks, entrances/exits, rally points, capture markers, and effect/audio attachment metadata.
- Added Ukrainian and Russian faction palettes, role-specific silhouettes, damage/destruction readability, fallback lookup behavior, exact deterministic manifests, and a generated review contact sheet.
- Added focused validation for provenance, schema, canonical technology-tree coverage, deterministic output, lifecycle completeness, silhouettes, metadata, fallbacks, and generated-output ownership.
- Registered the building-art verifier in the authoritative verification plan without changing active renderer composition, simulation, navigation, economy, campaign, UI, or audio ownership.

## Self-review

The complete task diff was reviewed for acceptance coverage, canonical building-ID drift, dependency direction, provenance, deterministic generation, generated-output ownership, lifecycle completeness, anchor and attachment metadata, fallback behavior, faction language, role readability, clipping, scope, and documentation.

Corrections made during review:

- Rebased the implementation onto current `main` after the original branch became stale, preserving every verifier stage added by intervening merges.
- Replaced an abandoned checked-in/generated-output approach with deterministic in-memory generation so source contracts remain authoritative and CI does not mutate the checkout.
- Used a temporary CI-only export step to generate the two faction atlases and complete contact sheet for direct visual review; that workflow-only change was removed before final verification.
- Kept the atlas behind the documented renderer handoff rather than installing a second building-rendering path or overstating runtime adoption.

Direct artifact review covered both faction atlases and the complete lifecycle contact sheet at approximately 96, 64, 48, and 32 pixels, plus normal color, grayscale, and red–green/deuteranopia simulations. Lifecycle progression was coherent; role silhouettes, icons, active accents, and damage/destruction states remained legible; no visible clipping was found. Corresponding faction roles intentionally share production-class geometry, so faction identity is weaker in pure grayscale than in color; faction palettes remained distinguishable in normal color and retained usable contrast in the red–green simulation. This limitation is recorded rather than treated as player verification.

No blocking defect remained after review.

## Verification

Clean synchronized implementation head `31839a8a939ca2eab8b9ef1f0e9f13a4ff891d41` passed GitHub Actions run `30975688971`:

- full assembled verifier passed;
- active-claim diagnostics passed;
- browser startup and first-mission smoke passed.

Temporary visual-export head `814436f43af4b3035c5db95b94dfbc71cd71703b` passed GitHub Actions run `30975797452` and produced review artifact `8918208504`:

- 985 Node tests passed; 0 failed;
- 396 ordered verifier stages passed;
- 16 buildings and 208 production frames verified;
- 210 runtime frames and 176 animations reproduced;
- 364,080 atlas bytes and 147,419 contact-sheet bytes reproduced deterministically;
- active-claim diagnostics passed;
- browser startup and first-mission smoke passed with a valid canvas and three mission cards;
- browser warnings: none;
- completion-evidence audit and diagnostics upload passed.

The final marker-inclusive branch must pass the same authoritative verifier, active-claim diagnostics, and browser smoke before merge.

## Runtime composition evidence

The source, generation, manifest, metadata, lookup, fallback, and verification contracts are production-ready for the renderer handoff. UFR-115 does not switch the current prototype building renderer to these atlases and does not add a competing runtime composition path.

## Browser/player evidence

Automated browser smoke proves the assembled application still starts the first mission successfully after the new modules are present. Direct review proves the generated source artifacts are visually coherent at the intended asset scales and tested color presentations. Because the atlas is not mounted in active missions or the Art & VFX Lab, this does not establish in-game placement, selection, capture, construction, destruction, or player-experience behavior.

## Save implications

No save, checkpoint, replay, profile, or migration schema changes are introduced. Building art is deterministic presentation metadata and generated SVG content.

## AI-system use

No AI planner, observation, doctrine, budget, production, targeting, or command behavior is changed. AI systems remain unaware of presentation assets.

## Evidence boundary

`CONTRACT_COMPLETE` is the highest justified level. The complete building source/runtime atlas family, provenance, deterministic generation, lifecycle states, icons, metadata, lookup, fallbacks, tests, and direct artifact review exist. Active renderer integration, mission/Art & VFX Lab captures, and player verification are not claimed; therefore `RUNTIME_INTEGRATED` and `PLAYER_VERIFIED` are not claimed.
