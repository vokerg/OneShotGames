# UFR-042 — Define deterministic area-damage policy

- PR: #55
- Completed commit: `9b79d6e2c203b675a50340cccc09c45d29becbce`
- Base: `c8716ebe7debd8be31728f72497b7de99078036c`
- Implementation head before task evidence: `97c3a3a9704c02de71aba7042c641652e6e54eb2`
- Parallel rule: YES — focused damage policy
- Dependencies verified on `main`: UFR-031, UFR-032

## Delivered

- Versioned immutable policy contracts for constant, linear, and quadratic splash falloff.
- Full-damage inner radius, exact outer-boundary inclusion, minimum falloff ratio, and footprint-aware distance.
- Disabled, full, and scaled friendly-fire policy.
- Disabled, full, and scaled structure-damage policy with deterministic multiplier composition.
- Stable target-ID ordering, duplicate rejection, skipped-target reasons, and no mutation of live entities.
- Explicit ownership: simulation applies damage; presentation consumes a reference-free effect descriptor.
- Focused ownership and integration guidance in `docs/AREA_DAMAGE.md`.

## Verification

- `node --check src/combat/area-damage-policy.js` — passed in the focused local mirror.
- `node --test tests/combat/area-damage-policy.test.mjs` — 9/9 passed.
- Coverage includes all falloff curves, inner/middle/boundary/outside distances, collision radii, zero-radius impacts, all friendly-fire modes, all structure modes, combined multipliers, deterministic ordering, effect ownership, and invalid inputs.
- Connector comparison against current `main` showed the branch ahead with no behind commits and changes confined to `ukrainian-front-rts/` task-owned files.
- Full checkout command attempted: `git clone --filter=blob:none --no-checkout https://github.com/vokerg/OneShotGames.git /tmp/OneShotGames`; it failed before checkout because the runtime could not resolve `github.com`, so native `bash verify.sh`, `./run.sh`, and browser mission checks could not run here.
- GitHub Actions reported no workflow runs for the implementation head.
- Browser playtesting is not applicable to the branch behavior itself because UFR-042 adds a pure policy and deliberately does not integrate it into projectile or renderer runtime paths.

## Notes

Projectile travel, hit resolution, damage application, smoke, cover, armor, renderer effects, and balance remain in their existing owners. A later integration owner must consume this policy rather than duplicate its rules.
