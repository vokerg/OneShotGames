# UFR-135 — Add production command card

- PR: #153
- Original base: `860a18ba5fbbedd27cea746c0f4efc60a8326500`
- Synchronized main: `f5aa93e97730aac2b2c070ada2f6b58cddbb9d3e`
- Implementation evidence head: `d1fa9411d833c7fdfa25dfd6c05e0ef4fb49b536`
- Evidence level: `RUNTIME_INTEGRATED`
- Parallel rule: `LIMITED — command-card hotspot`
- Dependencies verified: UFR-013, UFR-041, UFR-133

## Delivered

- Added the immutable versioned `fields-of-resolve.command-card` presentation contract.
- Added a deterministic 4-column by 3-row, twelve-slot paged model with stable grouping and duplicate-ID containment.
- Added orders, targeting, stances, abilities, construction, production, and modernization grouping while preserving source order inside each group.
- Added real hotkey labels, `aria-keyshortcuts`, player-facing disabled reasons, pressed stance/research state, and armed attack-move, attack-ground, construction, patrol, guard, and follow state.
- Added arrow, Home, End, PageUp, and PageDown navigation while retaining native Tab, Shift+Tab, Enter, and Space button behavior.
- Added Attack Ground to the active command card through the existing UFR-036 controller without creating new simulation semantics.
- Added a reversible UI capture/commit installer through the existing tactical command-card composition seam.
- Added responsive production styling, 32-pixel action and pager targets, reduced-motion handling, forced-colors handling, and narrow-panel-safe columns.
- Added focused model/DOM/lifecycle tests, a dedicated composition verifier, authoritative verifier registration, and ownership documentation.

## Diagnostic corrections

- Updated paged-state assertions to inspect the complete normalized action set rather than assuming all stance actions appear on page one.
- Kept production-card installation conditional on the active command DOM root so existing tactical-only unit fixtures retain their documented DOM-free contract.
- Synchronized with UFR-115 and preserved both building-atlas and command-card verification stages.

## Self-review corrections

- Replaced the fixed 82-pixel column minimum with zero-minimum responsive columns to prevent clipping in narrow command panels.
- Enforced the production skin's 32-pixel minimum target on command actions and page controls.
- Replaced invalid row/column ARIA indices on native buttons with a labeled toolbar pattern, deterministic geometry data attributes, and `aria-keyshortcuts`.
- Kept command delegates, simulation validation, named-action ownership, settings UI, and input rebinding unchanged.

## Verification

Authoritative workflow `30976825950` passed on implementation head
`d1fa9411d833c7fdfa25dfd6c05e0ef4fb49b536`:

- full assembled verifier passed;
- active claim diagnostics passed;
- browser startup and mission smoke passed;
- completion evidence audit passed;
- verification diagnostics upload passed.

Focused coverage is included in the assembled verifier:

- `node --test tests/ui/command-card.test.mjs` — model, paging, navigation, DOM, lifecycle, teardown, and validation coverage passed;
- `node scripts/verify-command-card.mjs` — grid, grouping, hotkeys, disabled reasons, targeting/stance states, responsive targets, toolbar semantics, styling, and active composition passed.

The implementation branch was zero commits behind `main` at the evidence boundary. The complete diff was limited to the command-card module, tactical composition seam, stylesheet, focused tests/verifier, documentation, and task records.

## Evidence limit

The active application installs the production command card through `src/main.js`, normal mission refresh renders it, and assembled browser mission smoke passed. This justifies `RUNTIME_INTEGRATED`.

This task does not claim `PLAYER_VERIFIED` or `RELEASE_VERIFIED`: no human matrix was completed across every unit/building selection, command combination, page, viewport, keyboard layout, pointer device, or assistive technology. UFR-141 retains key rebinding and broader accessibility settings ownership; UFR-142 retains complete viewport and high-DPI validation ownership.
