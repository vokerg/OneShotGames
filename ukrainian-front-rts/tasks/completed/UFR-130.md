# UFR-130 — Add voice pipeline

- PR: #143
- Final base commit: `7ae1c893db89b5268d3792018ed6c4d7a3da9964`
- Evidence level: `CONTRACT_COMPLETE`

## Acceptance evidence

- Added a versioned, deeply immutable voice-hook catalog covering unit acknowledgement, under-attack alert, and dynamic campaign-dialogue families.
- Added English and Ukrainian variants with exact, regional, declared, and default language fallback.
- Added deterministic fixed-tick repetition windows, retry ticks, request-keyed campaign dialogue, and recent-variant avoidance.
- Added independent voice, subtitle, and speaker-label preferences; voice opt-out preserves subtitles, and disabling all output does not consume repetition history.
- Added UFR-092-compatible narrative request conversion without importing or mutating UI state.
- Added a fail-closed `audio.request` adapter with immutable tick/sequence forwarding and exact subscription disposal.
- Added optional future binary asset preload, digest, decode, UFR-125 resolution, UFR-124 `voice`-bus playback, concurrency tagging, retry, and teardown boundaries.
- Added complete provenance validation for hook-only, synthetic, and recorded sources, including safe paths, hashes, duration, license/source/redistribution fields, and public-figure impersonation rejection.
- Added localized accessible hook-first acknowledgements and alerts plus dynamic campaign subtitle hooks without recorded third-party speech or public-figure voice imitation.
- Added authoritative `voice-hooks` verification and focused ownership/accessibility/provenance documentation.

## Self-review corrections

1. Unknown dynamic speakers are subtitle-only until an explicit reviewed speaker profile permits voice.
2. Playback failure with subtitles disabled no longer reports a successful silent delivery or consumes repetition history.
3. Pure resolver entry points normalize raw manifest objects instead of trusting schema/version fields alone.
4. An explicitly supplied incompatible mixer is rejected even when the current catalog contains only hook-first entries.
5. Catalog validation rejects fallback cycles, event-family drift, unsafe binary metadata, incomplete provenance, and public-figure speech assets.

## Verification

- Focused local reconstruction: **15 passed, 0 failed** across catalog, language, repetition, subtitle, opt-out, narrative, mixer, preload/retry, playback-failure, domain-adapter, and public-boundary tests.
- Authoritative corrected-head workflow: run `30883978743` passed:
  - full assembled verifier, including voice-hook/provenance verification;
  - active-claim diagnostics;
  - browser startup and first-mission smoke;
  - completion-evidence audit;
  - diagnostics upload.

## Evidence limits

The assembled application does not yet install the voice pipeline or contain audible speech binaries. UFR-131 retains settings/UI integration and UFR-132 retains full audio QA/provenance closure. This task therefore does not claim `RUNTIME_INTEGRATED` or `PLAYER_VERIFIED`.
