# UFR-008 — Expand architecture verification

- PR: #22
- Completed commit: `f6fa779b95f0b08ab5c4417128ef62263b90735b`
- Verification: exact connector-visible blobs matched local Git blob hashes `fdecd342bab2f88faa5b1030a4673ae32fba9f1b` (`scripts/lib/architecture-verifier.mjs`), `fae222d81cbda838d5fcf03724ec4545c68e14cd` (`scripts/verify-architecture.mjs`), and `1af2ee7fc4a507277fef5d4fee6ba1f86c1ee050` (`tests/tooling/architecture-verifier.test.mjs`). `node --check` passed for all three files, and `node --test tests/tooling/architecture-verifier.test.mjs` passed 8/8 tests covering static/side-effect/re-export/dynamic import extraction, valid topology including the UFR-010 core event layer, core/system/app boundary violations, imports outside `src/`, unclassified-module rejection, DOM leakage, audio ownership, schema ownership, and required fixed-step/composition imports. The branch was cleanly rebased onto `main` commit `a289df3f78d3e9111059356d1d3647a3801fe5f0` after UFR-028 merged. A native full-checkout `bash verify.sh` could not run because the execution environment cannot obtain the repository outside the GitHub connector; browser playtesting is not applicable to this tooling-only task.
- Notes: Replaces the monolithic architecture script with a reusable deterministic checker and thin CLI. Enforces explicit core/schema/config/systems/game/app/input/ui/render/audio/main dependency direction, repository-relative production imports, no imports outside `src/`, declared ownership for every source module, restricted DOM and direct audio access, single content-schema ownership, and required composition/fixed-step integration imports. Adds temporary-project tooling fixtures and documents the `tests/tooling/` layer. No runtime gameplay, content, input, rendering, UI, balance, save, or sibling-game behavior changes.