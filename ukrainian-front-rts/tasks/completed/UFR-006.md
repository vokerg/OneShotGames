# UFR-006 — Build a headless simulation harness

- PR: #15
- Completed commit: pending maintainer squash merge
- Verification: `node --check` passed for the exact connector-visible harness and simulation test blobs; the focused headless suite passed 5 tests covering DOM/canvas-free startup, temporary viewport restoration, structured command dispatch, fixed-tick movement, production/research results, deterministic same-seed snapshots, different-seed divergence, and actionable failures. The combined Node test layer passed 19 tests (14 existing unit tests plus 5 scenario tests). A reconstructed full `bash verify.sh` fixture based on current `main` passed task-queue tests (7 cases), queue validation (168 tasks), content schema validation (8 families), UFR-009 content-validator fixtures (7 cases), production-style content validation, seeded-random validation, and architecture validation. Connector inspection confirmed the production `Game.start`/`Game.update` viewport reads, public command methods, Donbas initial state, `uaInfantry` cost and barracks production access, thermal upgrade cost, and seeded wave behavior used by the scenarios. The branch was rebased onto current `main` commit `8030c10be270b5809579a38942dd16d57e19ddb7` after UFR-009 merged. The connector environment does not expose a native repository checkout or browser session, so the real-checkout command, `./run.sh`, and manual control regression could not be executed.
- Notes: Adds an app-layer headless harness that constructs `Game` through an injectable factory, derives and resets mission seeds, temporarily supplies numeric viewport globals only while invoking current `Game.start`/`Game.update`, dispatches structured commands to public game methods, advances an immutable tick duration, resolves entity IDs, provides predicate assertions, and emits reference-free deterministic snapshots. Adds `tests/sim/` coverage and documents the command/snapshot contract, test-layer ownership, process-global random-stream isolation, and the explicit boundary that UFR-007—not this harness—owns fixed-step simulation phases. Runtime content, combat rules, balance, browser input, rendering, UI, and `verify.sh` are unchanged.
