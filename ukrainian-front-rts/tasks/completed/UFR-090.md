# UFR-090 — Add trigger-safe mission checkpoints

- PR: #101
- Completed commit: `14b76bdd00e7ec573add61759bc18725da0bf91a`
- Acceptance: Adds immutable, versioned checkpoint envelopes; per-operation enable/disable policy; deterministic bounded retention and latest-checkpoint selection; exact operation/profile/script compatibility gates; round-trip adapters for the UFR-085 mission-state save shape; and an injected runtime that pauses, clears transients, atomically applies authoritative mission and trigger state, and resumes in `finally`.
- Verification: `node --check src/core/mission-checkpoint-service.js`; `node --check src/app/mission-checkpoint-runtime.js`; `node --check tests/campaign/mission-checkpoint-service.test.mjs`; `node --check tests/campaign/mission-checkpoint-runtime.test.mjs`; `node --test tests/campaign/mission-checkpoint-service.test.mjs tests/campaign/mission-checkpoint-runtime.test.mjs` (13 passed, 0 failed). Pushed source, tests, and documentation blobs were compared with the locally verified blobs and matched exactly.
- Verification limitations: Native full-repository `bash verify.sh`, `./run.sh`, and browser mission playtesting were unavailable in the connector-only environment. Browser controls and mission-authored checkpoint placement are explicitly deferred in `docs/MISSION_CHECKPOINTS.md`.
- Notes: UFR-085 remains storage/slot/autosave owner and UFR-086 remains mission-script execution owner. This task does not change campaign save internals, trigger evaluation order, live UI, mission content, simulation balance, or sibling games.
