# UFR-147 — Add deterministic replay recording and playback

- PR: #176
- Evidence level: `RUNTIME_INTEGRATED`
- Acceptance: Records version/compatibility metadata, seed, commands, choices, periodic/final checksums, final tick, and outcome in a canonical versioned replay; playback reconstructs the scenario deterministically, detects command/checksum divergence, supports seek/step/scrub, and emits self-contained defect reports.
- Runtime integration: `src/app/replay-runtime.js` wraps the existing public simulation harness and preserves its sole tick owner. Same-tick commands and choices invalidate prior checksums so final-state evidence cannot be suppressed.
- Core format: `src/core/replay.js` validates plain JSON data, canonicalizes key order, serializes deterministically, computes stable checksums, enforces compatibility, and builds timeline/defect-report records.
- Focused verification: `tests/core/replay.test.mjs` covers canonical round trips, malformed data, compatibility rejection, command and checksum divergence, same-tick choice final checksums, timeline controls, and a record/playback round trip through the assembled real simulation harness.
- Documentation: `docs/DETERMINISTIC_REPLAY.md` records format ownership, recording/playback semantics, divergence behavior, scrubbing, and defect-report contents.
- Assembled verification: GitHub Actions run `31070679619` passed for implementation head `b673969b36ee3caf4d244f62e89a4e9944c3b49e`; the full assembled verifier and browser diagnostics completed successfully.
- Scope: No simulation-clock duplication, balance changes, multiplayer, save-service replacement, campaign content, art, audio, broad UI redesign, or sibling-game behavior is changed.
