# UFR-005 — Add dependency-free unit-test conventions

- PR: #14
- Completed commit: pending maintainer squash merge
- Verification: `node scripts/run-tests.mjs` passed 14 tests across four files; `node scripts/run-tests.mjs math objectives` passed 7 focused tests; an unmatched filter exited 1 with an actionable message; syntax checks passed for every new `.mjs` file; `bash -n verify.sh` and trailing-whitespace checks passed. The complete `bash verify.sh` sequence passed in a reconstructed connector fixture containing the exact branch test/runner/verification blobs, exact prerequisite verifiers and task queue, and current production seams copied from connector-confirmed `Game.canAfford`, `Game.pay`, `Game.research`, objective evaluation, and upgrade data. Outputs included task-queue tests (7 cases), queue validation (168 tasks), content schema validation (8 families), seeded-random validation, and architecture validation. The connector environment does not expose a complete repository checkout or browser session, so the command could not be rerun against a native checkout and no manual mission regression was performed; browser playtesting is not applicable to this tooling-only change.
- Notes: Adds a dependency-free wrapper around Node's built-in test runner with recursive stable discovery, path-fragment filtering, and non-zero empty/unmatched/failure behavior. Adds 14 tests covering clamp/distance/seeded ranges, affordability and payment mutation, upgrade prerequisite and insufficient-funds behavior, and objective evaluation for all three missions plus unknown-mission failure. `verify.sh` now syntax-checks test files and runs the unit suite before specialized verifiers. Documents test layout, fixture isolation, deterministic-service reset, and test-layer dependency direction. Runtime, content, balance, save schema, art, and audio behavior are unchanged.
