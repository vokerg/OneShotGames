# UFR-011 — Make one unified verification command

- PR: #32
- Completed commit: pending maintainer squash merge
- Verification: `node --check scripts/lib/verification-runner.mjs`, `node --check scripts/run-verification.mjs`, and `node --check tests/tooling/verification-runner.test.mjs` passed. `node --test tests/tooling/verification-runner.test.mjs` passed 5/5 tests covering stable recursive syntax discovery, authoritative stage order, complete success, first-failure short-circuiting with exact non-zero status preservation, and invalid executor-result normalization. Shell-level synthetic projects then ran the exact `bash verify.sh` entry point: the complete success fixture reached the final architecture stage and exited 0; an intentional content-validator fixture failure exited 9, identified `content-fixtures`, and prevented every later stage from running. The branch is zero commits behind `main` commit `9374635eb4b93059e5eef5b9e634223d926ac6b8` and changes only verification tooling, tests, documentation, and the completion marker. A native full-repository `bash verify.sh` run remains unavailable because the execution environment cannot resolve `github.com` outside the GitHub connector; browser playtesting is not applicable to this tooling-only task.
- Notes: Makes `bash verify.sh` the single top-level verification command. The shell entry point delegates to a reusable Node runner that owns stable syntax discovery, ordered unit/simulation and contract stages, fail-fast execution, exact non-zero status propagation, and actionable stage labels. CI should invoke the same shell command rather than duplicate the stage list. No runtime gameplay, content, navigation, combat, input, rendering, UI, balance, save, or sibling-game behavior is changed.
