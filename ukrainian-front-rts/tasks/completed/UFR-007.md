# UFR-007 — Document and enforce fixed-step simulation phases

- PR: #19
- Completed commit: pending maintainer squash merge
- Verification: connector-exported copies of `src/core/fixed-step-clock.js`, `src/app/runtime.js`, and `src/systems/simulation-phases.js` passed `node --check`. Reconstructed focused Node suites passed 9/9 tests covering 60 FPS, 10 FPS, and irregular frame chunking; fixed 30 Hz tick generation; mission-restart accumulator reset; long-frame capping; timing validation; explicit phase order; victory precedence after objective evaluation; and game-over no-mutation behavior. `tests/sim/fixed-step-determinism.test.mjs` and all new test files passed syntax checks, and connector review confirms the headless scenario drives the same seeded command stream through 30 fixed ticks before comparing reference-free snapshots. The branch was merged with current `main` commit `d0c28476676bedaa3eb7b71a46b81b1071c82564`; comparison shows it is zero commits behind and differs only in the ten UFR-007 files. A native full-checkout `bash verify.sh`, `./run.sh`, and browser playtest could not run because the execution environment cannot resolve `github.com`, and no GitHub Actions workflow run is configured for the PR head.
- Notes: Adds a reusable 30 Hz accumulator, caps accepted render-frame delta at 250 ms, resets partial time on mission start, routes browser elapsed time through zero or more fixed ticks, delegates `Game.update` to an explicit clock/camera/units/projectiles/production/waves/cleanup/objectives/outcome phase contract, adds unit/runtime/headless frame-chunking coverage, and documents phase ownership and extension rules. No content, balance, navigation, combat, economy, rendering, or UI feature behavior is added.
