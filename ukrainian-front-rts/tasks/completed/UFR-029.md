# UFR-029 — Create navigation torture maps

- Original implementation PR: #121
- Completion reconciliation PR: #161
- Original implementation merge: `65273f977d18a07387b928fa88665f937acf278a`
- Reconciliation base: `6a3ab67b8056fe5dadbc2dbfcb5b3d31d07ca12b`
- Evidence level: `CONTRACT_COMPLETE`
- Parallel rule: `YES — test content only`

## Delivered

- Added an immutable bridge-crossing fixture that forces ground routes through one authored bridge cell.
- Added a base-gate fixture with deterministic forward and reverse routing through the only opening.
- Added a dense-group fixture with 36 start/destination pairs, request-order independence, bounded routes, no repeated-cell oscillation, and explicit search/cache metrics.
- Added a transport fixture covering deterministic crossing, stable embark order, valid disembark placement, and non-overlapping exits.
- Added before/after destruction fixtures that invalidate the old route, enforce bounded repath throttling, and recover through the newly opened cell.
- Added before/after dynamic-construction fixtures that invalidate the direct path and route through the newly authored gate.
- Added deterministic replay/state equality and bounded route assertions across the scenario family.

## Reconciliation findings

PR #121 merged the fixture and focused tests but accidentally retained `tasks/claims/UFR-029.md` and omitted `tasks/completed/UFR-029.md`. This made the derived queue report UFR-029 as READY even though its implementation had merged.

The original PR head failed its historical workflow because the navigation failure-metric contract was incomplete. Recovery PR #123 restored that metric without changing route behavior, and PR #125 subsequently completed the UFR-030 assembled navigation gate with UFR-029 as a verified dependency.

The current-main audit found no missing UFR-029 acceptance case and required no fixture, navigation-system, or gameplay change.

## Verification

The authoritative test runner discovers every `tests/**/*.test.mjs`, including:

```text
tests/navigation/navigation-torture-scenarios.test.mjs
```

The completion reconciliation was verified through the full assembled `bash verify.sh`, active-claim diagnostics, browser startup and first-mission smoke, completion-evidence audit, and diagnostics upload. Exact implementation and marker-inclusive workflow runs are recorded in PR #161.

Relevant current implementation blobs:

- `3f48093d3f2b73b7b4e92b5c8442b33e92ae78de` — `tests/fixtures/navigation-torture-maps.js`
- `e82d08edb43d78dffd11ae8abc4faa56fc7dc4b7` — `tests/navigation/navigation-torture-scenarios.test.mjs`

## Evidence boundary

`CONTRACT_COMPLETE` is the highest justified UFR-029 level because the task owns deterministic scenario fixtures and focused integration tests rather than active player-facing runtime composition. Downstream UFR-030 separately records `RUNTIME_INTEGRATED` evidence for the assembled navigation gate. No manual player verification is claimed here.

## Scope

No navigation algorithm, simulation behavior, balance, UI, art, audio, campaign, save, replay, AI, or sibling-game behavior changed. The reconciliation only repairs durable task state by adding this marker and removing the obsolete claim file.
