# UFR-010 — Add a domain event stream

- PR: #25
- Completed commit: pending maintainer squash merge
- Verification: `node --check src/core/events.js`; `node --check tests/core/domain-events.test.mjs`; `node --test tests/core/domain-events.test.mjs` (5 tests passed). Connector review confirms changes are restricted to `ukrainian-front-rts/`. Full `bash verify.sh` was not executable because the connector session does not expose a repository checkout; the focused files were reproduced exactly in a temporary local tree for syntax and test execution. Browser playtesting is not applicable because this task adds a dependency-free core contract with no player-visible behavior.
- Notes: Adds a frozen event taxonomy for shots, impacts, deaths, production, research, objectives, alerts, audio requests, telemetry, and replay records. The stream provides deterministic tick/sequence ordering, immutable payload snapshots, synchronous typed/wildcard subscriptions, and ordered peek/drain buffering. Simulation producers and read-only consumers are documented; gameplay rules and presentation behavior are unchanged.
