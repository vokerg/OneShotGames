# UFR-136 — Add minimap and alert queue

- Pull request: #154
- Branch: `ufrts/UFR-136-minimap-alerts`
- Original base: `860a18ba5fbbedd27cea746c0f4efc60a8326500`
- Reviewed implementation head: `8d006cec6761da326481fe374c2d66074b37a78b`
- Claim-free evidence head: `3e6e3d6e82687f75ccc9916a040727acbf0872dd`
- Evidence level: `RUNTIME_INTEGRATED`

## Delivered

- Replaced the legacy minimap presentation with an immutable snapshot-driven renderer for all authored terrain cells, roads, explored/visible/hidden fog, filtered unit/building/resource markers, visible hostile contacts, ally/neutral identity, active pings, and the clipped camera viewport.
- Added native unit, building, resource, ally, and neutral marker filters without changing simulation or visibility authority.
- Added a deterministic, bounded, priority-ordered, deduplicated alert queue for attack, objective, production, and explicit informational alerts.
- Added queue-entry and minimap-ping camera focus while preserving ordinary minimap navigation through the existing battlefield-input owner.
- Added mission/reset rebasing so stale alerts and explored-fog presentation do not cross mission boundaries or simulation-time rewinds.
- Added accessible canvas naming, labelled native filters, a live alert region, keyboard-focusable location actions, geometry-reinforced marker identity, and deterministic reduced-motion-safe ping presentation.
- Mounted the feature through application composition only, replacing the active renderer instance's `mini()` method and restoring renderer, toast, API, listeners, and queue DOM state during teardown.
- Added ownership documentation and focused model/runtime/teardown tests.

## Self-review

The complete task diff was reviewed for acceptance coverage, fog-information leakage, visibility authority, camera/input ownership, runtime composition order, alert duplication, mission reset behavior, production sequencing, filter behavior, bounded queue semantics, DOM accessibility, responsive layout, teardown restoration, dependency direction, and scope isolation.

Corrections made during review:

- Reworked the alert queue into a bounded overlay above the existing 220 × 138 minimap frame so it does not expand or squeeze the command panel.
- Added mission-identity and simulation-time rewind rebasing to prevent stale alerts or explored fog from crossing restarts.
- Restricted toast-based alert creation to explicit metadata handoff so observer-generated damage, objective, and production alerts are not duplicated by ordinary status messages.
- Required explicit neutral identity so missing or unknown team values cannot be mislabeled as neutral contacts.
- Stabilized alert DOM refreshes and preserved ordinary minimap navigation outside active ping hit areas.
- Restored the exact pre-install alert-queue markup and class state during disposal and added a regression test for that teardown contract.

No blocking defect remained after review.

## Verification

Reviewed implementation head `8d006cec6761da326481fe374c2d66074b37a78b` passed GitHub Actions run `30977146915`:

- full assembled verifier passed;
- active-claim diagnostics passed;
- browser startup and mission smoke passed;
- completion-evidence audit passed;
- diagnostics upload passed.

GitHub generated a clean synthetic merge result with the then-current `main`; the only intervening main commit was the isolated UFR-115 building-atlas completion and no changed file overlapped this task.

The completion marker and claim removal are present together on claim-free evidence head `3e6e3d6e82687f75ccc9916a040727acbf0872dd`. The final marker-inclusive branch head must pass the same authoritative workflow before merge.

## Runtime composition evidence

`src/main.js` installs one `minimap-alerts` application module. The module consumes existing game, visibility, camera, renderer, UI, mission, objective, and production state; it does not create another render loop, replace simulation updates, evaluate mission outcomes, calculate combat targeting, or mutate authoritative fog state.

Focused tests cover world/minimap coordinate conversion, viewport clipping, 80 × 52 terrain/fog coverage, hostile fog exclusion, relationship and marker filters, road normalization, queue priority/bounds/deduplication/expiry, damage alerts, explicit toast handoff, mission reset rebasing, camera focus, renderer replacement, API/toast restoration, and exact queue-DOM teardown restoration.

## Browser/player evidence

Automated browser smoke proves the assembled application starts a mission successfully with the minimap module installed and the active canvas/runtime composition intact. A human-reviewed all-mission visual matrix at every supported zoom, grayscale mode, and color-vision preset was not performed, so this marker does not claim `PLAYER_VERIFIED`.

## Save implications

No save, checkpoint, replay, campaign-profile, or migration schema is changed. Explored minimap presentation and alerts are transient runtime state and are deliberately reset when the mission identity changes or simulation time rewinds.

## AI-system use

No AI planner, observation, doctrine, budget, production, targeting, visibility, or command behavior is changed. AI systems remain unaware of the presentation-only minimap and alert adapter.

## Evidence boundary

`RUNTIME_INTEGRATED` is the highest justified level. The assembled application mounts and exercises the intended minimap/alert contract through its production composition path, and automated integration/browser smoke passes. Human all-mission visual and interaction review was not performed; therefore `PLAYER_VERIFIED` and `RELEASE_VERIFIED` are not claimed.
