# UFR-120 — Replace prototype styling with a production UI skin

- PR: #147
- Base: `8d1fc031e5fa3e2154a54c8234116cbf9abdd8b4`
- Implementation evidence head: `b6f62737ca68f62d84c8cdedef6526fd686c5128`
- Evidence level: `RUNTIME_INTEGRATED`
- Parallel rule: `LIMITED — styles.css/UI asset hotspot.`
- Dependencies verified: UFR-106 and UFR-119

## Delivered

- Added the immutable versioned `fields-of-resolve.ui-skin` contract and shared production UI tokens.
- Added nine deterministic, text-free, crisp-edge SVG nine-slice assets for panels, overlays, operation screens, controls, tabs, tooltips, scrollbars, and a visible diagnostic fallback.
- Added exact SVG generation/check tooling and a dedicated source/provenance manifest.
- Mounted `ui-skin.css` after the existing component styles in the active browser entry point.
- Replaced prototype presentation across the top bar, command panel, selection UI, objectives, economy HUD, operation book, mission cards, messages, tooltips, scrollbars, and endgame report without changing DOM or simulation ownership.
- Added complete default, hover, active, focus-visible, disabled, selected, researched, benefit, warning, danger, reduced-motion, high-contrast, and forced-colors treatment.
- Added focused contract tests, dedicated runtime-composition verification, and production ownership documentation.

## Self-review corrections

- Replaced an opaque generated data-URI stylesheet proposal with reviewable committed SVG assets while retaining deterministic generation.
- Explicitly cleared legacy background images so prototype gradients cannot show beneath production border assets.
- Repositioned top-bar and economy-header tooltips below their controls so they remain inside the viewport.
- Kept all reusable frames text-free and retained live DOM labels for localization and accessibility.
- Preserved the current semantic UI/component architecture rather than introducing a second renderer or application installer.

## Verification

Authoritative workflow run `30935318098` passed on implementation head
`b6f62737ca68f62d84c8cdedef6526fd686c5128`:

- full assembled verifier passed;
- active-task claim diagnostics passed;
- browser startup and mission smoke passed;
- completion-evidence audit passed;
- verification diagnostics upload passed.

Focused evidence also passed:

- `node scripts/build-ui-skin.mjs --check`;
- `node scripts/verify-ui-skin.mjs` — 9 scalable assets and 9 component mappings verified;
- `node --test tests/ui/ui-skin.test.mjs` — 5 tests passed.

## Evidence limit

The production stylesheet is loaded by `index.html` and the assembled browser mission smoke passed, so this task is `RUNTIME_INTEGRATED`. It does not claim `PLAYER_VERIFIED` or `RELEASE_VERIFIED`: no human visual matrix was completed across every supported resolution, zoom level, grayscale/color-vision mode, and UI screen. UFR-122 retains deterministic screenshot-scene and complete visual-regression ownership; UFR-141 and UFR-142 retain broader accessibility and viewport validation ownership.
