# UFR-013 — Introduce named actions and configurable key bindings

- PR: #17
- Completed commit: `462a109ee233918e2980cdea3ad34d06270e24c8`
- Verification: `node --check src/input/action-map.js`; `node --test tests/unit/input-action-map.test.mjs` (3/3 passing). Connector review confirmed that battlefield keydown/keyup handling resolves configurable physical keys to named actions, held camera actions preserve the existing `Game.keys` contract, command actions retain repeat guards, blur clears held state, defaults remain immutable, and existing WASD/arrow/Escape/Q/X/T controls remain mapped. `bash verify.sh` and browser playtesting could not run because the available environment cannot resolve `github.com` to obtain a checkout and exposes no browser session.
- Notes: Adds the focused `src/input/action-map.js` boundary, configurable case-insensitive overrides/unbinding, input-adapter integration, deterministic unit coverage, and `docs/INPUT_BINDINGS.md`. No content schema, simulation, rendering, UI layout, or balance changes.
