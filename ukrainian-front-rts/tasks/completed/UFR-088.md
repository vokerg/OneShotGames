# UFR-088 — Create authored map format and loader

- PR: #88
- Completed commit: pending maintainer merge
- Base: `a16d509761e53102312397fea60bfb740285ea57`
- Acceptance: Adds a versioned dependency-free authored-map contract and loader covering terrain, integer elevation rows, per-layer passability overrides, roads, water, bridges, props, resources, start groups, rect/circle/polygon regions, UFR-086 trigger payloads, and JSON-compatible metadata. Validation is non-mutating and path-specific; successful loads are deeply frozen, canonical, reference-free snapshots with fixed `base → road → water → bridge` overlay order and a UFR-018-compatible navigation projection.
- Compatibility: Accepts legacy `spawns` and `decorations` source names and normalizes them to `starts` and `props`; ambiguous dual definitions are rejected. Current missions remain on their existing battlefield fallback until their authored mission tasks migrate them.
- Verification: `node --check src/core/authored-map.js`; `node --check tests/campaign/authored-map.test.mjs`; `node --test tests/campaign/authored-map.test.mjs` — 8 passed, 0 failed. A 24,000-cell map loaded 25 times in 38.675 ms total, averaging 1.547 ms per load in the focused local reconstruction.
- Exact verified blobs: `754a1e2ac9ff76e3e8f74132df39af52052be426` (`src/core/authored-map.js`); `f6c285f4479dffe567be59a29021681e83a88bf8` (`tests/campaign/authored-map.test.mjs`); `8a574a0b65a7b4ca4cb500bf209d23bc7b8caa63` (`docs/AUTHORED_MAP_FORMAT.md`). Connector-visible blob SHAs match the locally verified Git object hashes.
- Verification limitations: Native full-repository `bash verify.sh`, `./run.sh`, and browser interaction testing were unavailable because the execution environment cannot obtain a repository checkout outside the GitHub connector. Browser playtesting is not applicable to this browser-independent contract because no mission or runtime path is wired to it in this task. No GitHub Actions runs or commit status checks were configured for the PR head.
- Scope: No mission rebuild, trigger execution, navigation algorithm, terrain rendering, editor UI, combat, economy, balance, audio, or sibling-game behavior is changed.
