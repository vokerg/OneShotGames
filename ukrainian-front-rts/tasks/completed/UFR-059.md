# UFR-059 — Add rally points and deterministic production exits

- PR: #74
- Completed commit: pending maintainer squash merge
- Acceptance: Adds per-building rally points, Shift-appended rally waypoint queues, deterministic rally-oriented spawn-side selection, bounded blocked-exit fallback, live-unit occupancy rejection, zero-time queue retention while all exits are blocked, retry-safe reservation and repeat-production semantics, produced-unit FIFO rally-order inheritance, globally sequenced immutable deployment acknowledgements, capture-phase rally input, blocked-exit status, and player-facing deployment feedback.
- Verification: Exact connector-visible source blobs matched the focused local mirror: `72cb32990323795d8044612ee3bd8456f4cdd9a1` (`src/systems/production-exit-system.js`), `abd8923b093fae6f9444ec1cee44bd9f0bb8498c` (`src/systems/production-queue-system.js`), `909b4e67ba799e271962b514822d362589da4140` (`src/input/production-rally-input.js`), `6ba4eb16cc74b72a2b93ee39b6373d53d41953be` (`src/ui/production-exit-feedback.js`), `f64e75ddb2e3cdec9ee4e1663df28116b010d1c1` (`src/main.js`), and `dc16aac3767d9ecae5bd66906def49b9963b032a` (`tests/economy/production-exit-system.test.mjs`). `node --check` passed for all affected JavaScript files. `node --test tests/economy/production-exit-system.test.mjs` passed 16/16 tests. A focused benchmark completed 10,000 deterministic exit resolutions in 37.40 ms (0.0037 ms per call) in the local mirror.
- Verification limitations: Native full-repository `bash verify.sh`, the existing production-queue test file in a complete checkout, `./run.sh`, and browser mission playtesting were unavailable in the connector-only environment. The required browser checklist is recorded in `docs/PRODUCTION_EXITS.md`.
- Coordination: UFR-059 consumes `synchronizeNavigationGrid(game)` but does not edit navigation movement, path-service caching, worker gathering, construction progress, technology, art, audio, or sibling games. Queue timing, reservations, cancellation/refunds, and repeat ownership remain in `production-queue-system.js`; completion now commits only after an installed exit delegate returns a spawned unit.
