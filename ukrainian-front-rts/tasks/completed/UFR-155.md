# UFR-155 — Release packaging and reproducible static deploy

Status: DONE

## Delivered

- Deterministic runtime-only static package built from `index.html`, top-level CSS, `assets/`, and `src/`.
- Content-derived release IDs plus versioned web manifest, bootstrap, service worker, cache namespace, release metadata, and complete SHA-256 package manifest.
- Provenance-aware packaging gate reuses the UFR-153 release provenance verifier before emitting deploy artifacts.
- Offline precache is declared and validated against the complete packaged runtime file set.
- Reproducibility verification builds independent packages and requires byte-for-byte equality.
- Package verification fails closed on file mutation, undeclared files, and destructive output/source overlap.
- Release build/deploy/cache/rollback workflow documented in `docs/RELEASE_PACKAGING.md`.

## Verification

- Exact implementation head `2cb60a75d3819af2ded638ac44f3de055e4c656e` ran GitHub Actions workflow `31457883671`.
- Release-package unit/regression coverage passed, including deterministic rebuilds, digest drift, undeclared output, runtime-only input selection, offline declaration, and source-overlap protection.
- Chromium startup/vehicle/accessibility/readability/paired-readability/defensive/mobile gates and verification artifact completed successfully on that workflow.
- The assembled verifier reported 1235/1236 tests passing; its sole failure was the pre-existing unrelated `150 mixed units cross authored chokepoints deterministically within navigation budgets` timing-budget assertion at approximately 5.0 s. The navigation threshold was not changed or bypassed.

## Self-review

Reviewed package path safety, deterministic ordering, generated-name/version consistency, manifest/digest closure, service-worker cache ownership, provenance gating, and exclusion of authoring/task/test/documentation trees. A review-discovered destructive output-overlap defect was fixed before closeout and protected by regression coverage.
