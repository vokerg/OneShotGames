# UFR-016 — Add camera navigation controls

- PR: #23
- Completed commit: pending maintainer squash merge
- Verification: local focused mirror passed `node --check src/input/camera-navigation.js` and `node --test tests/input/camera-navigation.test.mjs` (5/5 tests). Coverage includes selected-center calculation, edge scrolling, middle-drag panning, camera bookmark save/recall, focus-selected, and per-feature settings toggles. Connector diff review confirmed the change remains within camera/input files and tests. Full `bash verify.sh`, browser playtest, and native checkout remain unavailable because this environment cannot resolve `github.com` outside the GitHub connector.
- Notes: Shift+F1–F4 stores camera bookmarks; F1–F4 recalls them; Space focuses the current selection; edge scrolling and middle-drag panning are enabled by default and individually configurable through `cameraSettings`. Existing WASD/arrow-key movement, wheel zoom, minimap navigation, control groups, and selection subgroup behavior are preserved. No simulation lifecycle, movement, combat, content, balance, save, replay, renderer, or sibling-game files are changed.
