# UFR-086 — Build data-driven mission scripting

- PR: #62
- Completed commit: pending maintainer squash merge
- Acceptance: Adds a versioned deterministic mission-script contract with validated timer, region, entity, resource, objective, and variable conditions; ordered dialogue, reinforcement, camera, weather, resource, objective, trigger-control, and victory/defeat actions; delayed actions, cooldowns, stable records, and scripted-objective mode.
- Verification: Exact local Git hashes were recorded for the scripting owner, fixed-step integration, focused tests, and documentation. `node --check src/systems/mission-script-system.js`; `node --check src/systems/simulation-phases.js`; `node --check tests/campaign/mission-script-system.test.mjs`; `node --test tests/campaign/mission-script-system.test.mjs` (12 passed, 0 failed). A focused 100-trigger/3,000-tick benchmark completed in approximately 237.3 ms total, about 0.079 ms per tick.
- Verification limitations: Native full-repository `bash verify.sh`, `./run.sh`, and browser mission playtesting were unavailable because the execution environment cannot obtain the repository outside the GitHub connector. Existing mission data contains no UFR-086 scripts, so no current player-visible mission behavior changes.
- Notes: The existing fixed-step phase list is unchanged. Mission scripts execute inside the objectives phase before legacy objective evaluation; missions opt into script-owned objectives with `objectiveMode: 'scripted'`. Dialogue/camera queues are presentation requests, weather is authoritative state, and `missionScriptRecords` is a drainable diagnostic/replay record rather than a domain-event stream. UFR-087 retains objective-library content, UFR-089/UFR-092 presentation consumers, and UFR-090 checkpoint restoration.
