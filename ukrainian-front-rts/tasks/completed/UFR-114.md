# UFR-114 — Produce both factions’ fires and support visual sets

- Final PR: #226
- Completion evidence: **RUNTIME_INTEGRATED**
- Base: `4553fc8581818f0c31c9550fcfd18d96145f2a63`

## Delivered

- 32 canonical support visual identities (18 Ukrainian, 14 Russian) locked to the merged UFR-073/UFR-074/UFR-077/UFR-078 content catalogs;
- complete faction coverage for drones, artillery, rockets, air defense, logistics, command, bridging, and support families;
- deterministic 64×64 production atlas geometry with eight directions and UFR-109 lifecycle coverage (`idle`, `move`, `attack`, `hit`, `damaged`, `death`, `wreck`), plus portraits and icons;
- 3,137 logical frames and 224 identity/state animation definitions with deterministic source validation and provenance;
- bounded lazy per-frame runtime decoding instead of monolithic full-atlas image decoding, with strict canonical/legacy alias ownership and fallback to prior renderer layers while frames load;
- battlefield and portrait renderer integration through the viewport runtime bootstrap without seizing unrelated infantry/vehicle identities;
- byte-parity tests proving lazy runtime frame SVGs match the production atlas geometry across all profiles and representative lifecycle/direction samples;
- dedicated four-page UFR-114 Art Lab review surface, deterministic automated color/grayscale capture, and independent support-atlas readiness so unrelated atlas loads cannot block support evidence;
- original repository-authored fictional source provenance under CC0-1.0 with no external visual assets.

## Verification

Exact-head workflow `31406391723` on `3cdfe24a92c351e9bec4dacbba37adf186c2160f` passed the full assembled verifier, active-claim diagnostics, browser startup/mission smoke, UFR-110 Art Lab review, Ukrainian infantry actual-mission review, paired vehicle actual-mission review, visual-regression capture including the UFR-114 review pages, audio autoplay/resume smoke, accessibility smoke, localization smoke, and completion-evidence audit.

Self-review additionally corrected two evidence/runtime-review defects before closeout: UFR-114 Art Lab readiness is no longer coupled to unrelated atlas loads, and automated grayscale capture uses a bounded browser filter rather than rescanning the full canvas every animation frame.

The completion level is recorded conservatively as `RUNTIME_INTEGRATED`: the visual sets are installed in the live renderer and browser-reviewed in Art Lab, while this task does not claim a dedicated support-unit actual-mission readability review beyond the repository-wide mission smoke gates.

The closeout commit is followed by one final exact-head workflow before squash merge.
