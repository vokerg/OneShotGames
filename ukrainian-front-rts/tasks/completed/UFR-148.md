# UFR-148 — Add batch balance simulation and telemetry

- Pull request: #179
- Branch: `ufrts/UFR-148-balance-simulator`
- Evidence level: `CONTRACT_COMPLETE`
- Verification follow-up: #181
- Merge waiver: maintainer instruction on 2026-08-06 to review, repair, and squash-merge the three active PRs despite the inactive Actions runner

## Delivered

- Added deterministic combat, economy, and mission-timing batches through the existing headless simulation harness.
- Added outcome counts and rates, duration summaries, numeric metric distributions, per-trial seeds, versioned snapshots, canonical serialization, and a dependency-free CLI.
- Kept authoritative simulation and balance values unchanged.
- Added recursive privacy validation for exported context, trials, metrics, notes, and snapshots.
- Repaired the reviewed privacy boundary so delimiter-separated and camelCase identifiers such as `user_email`, `emailAddress`, `userId`, and `sessionToken` are rejected in nested data.

## Verification evidence

Focused tests cover deterministic batch repetition, seed derivation, outcome/rate aggregation, p50/p95 and metric summaries, canonical snapshots, source-revision scoping, recursive privacy rejection, fake-harness command execution, resource accounting, and all three default batch kinds.

The required repository workflow did not execute because GitHub Actions remained queued without an active runner. No full verifier or assembled-harness pass is claimed. Issue #181 owns authoritative `bash verify.sh` and deterministic repeated-suite validation on merged `main`.

## Runtime composition evidence

The balance adapter constructs the existing simulation harness, starts a scenario, issues public harness commands, advances the authoritative fixed-step clock, and reads snapshots. It does not patch lifecycle methods or add a second simulation clock.

## Browser/player evidence

No browser or player-facing flow is introduced. The CLI and assembled headless execution still require authoritative verification under #181.

## Save implications

The task does not read or mutate player saves. Exported balance snapshots use their own versioned schema and contain scenario measurements rather than account or player identity data.

## AI-system use

The default batches measure current assembled behavior but do not change AI doctrine, cadence, observations, commands, or balance. Batch output is evidence for later UFR-149 decisions, not an automatic balance mutation.

## Limitations

- The default command policies are deterministic measurement fixtures, not substitutes for broad matchup design or player playtests.
- Completion evidence must not be raised above `CONTRACT_COMPLETE` until #181 records passing integrated checks.
