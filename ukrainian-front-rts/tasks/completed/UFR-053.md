# UFR-053 — Add deterministic resource drop-off capabilities

- PR: #69
- Completed commit: pending maintainer squash merge
- Acceptance: Adds versioned explicit resource drop-off capabilities for HQs and logistics depots, operational-state validation, deterministic navigation travel-cost selection, reachable perimeter approaches, stable tie-breaking, navigation-revision caching, construction/mission lifecycle materialization, and safe unreachable cancellation without losing carried resources.
- Verification: `node --check src/systems/resource-dropoff-system.js`; `node --check src/main.js`; `node --check tests/economy/resource-dropoff-system.test.mjs`; `node --test tests/economy/resource-dropoff-system.test.mjs` (10 passed, 0 failed). Coverage includes registry validation, explicit/override capabilities, invalid building states, approach ordering, path-cost measurement, cost-over-distance choice, unreachable candidates, stable ties, fallback fixtures, controller lifecycle, new buildings, mission restart, approach movement, and no-reachable-drop-off handling.
- Verification limitations: Native full-repository `bash verify.sh`, `./run.sh`, and browser mission playtesting were unavailable because the connector environment cannot obtain a repository checkout. Existing missions remain compatible; browser composition installs the new controller and current worker-gather tests are preserved by the unchanged UFR-051 public contract.
- Notes: `src/systems/resource-dropoff-system.js` owns capability materialization and destination choice. UFR-051 retains worker orders/gathering, UFR-052 worker UI, UFR-054 resource rates/capacity/depletion, UFR-057 building lifecycle, and navigation modules all pathability and terrain-cost rules. No balance, worker UI, extraction, construction placement, rally, campaign, renderer, audio, or sibling-game behavior is included.
