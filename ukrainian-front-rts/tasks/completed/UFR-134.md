# UFR-134 — Add selection panel

- PR: #137
- Original base commit: `85b7f402e42b595eb46513b84ba1f942fa0a5610`
- Latest server merge base at closure: `2166e9a8af0636f8c1b4fcc73b9190d67f7b728c`
- Final implementation head before completion marker: `27e75fb6bbdb7e76428b2b76baa50985919d6d44`
- Evidence level: `RUNTIME_INTEGRATED`
- Parallel rule: `LIMITED — selection UI owner.`
- Dependencies verified: UFR-015, UFR-045, and UFR-133

## Delivered

- Deeply immutable, versioned selection-panel presentation model.
- Deterministic primary-selection ordering and subgroup descriptors.
- Selected unit and building grid with health meters and health-state presentation.
- Status overlays for construction, damage, disabled/burning, morale, suppression, pinned, embarkation, garrison, and hold-fire state when exposed by authoritative entities.
- Veterancy rank, badge, XP, and progress through the existing veterancy contract.
- Read-only transport passenger and garrison occupant summaries with authoritative slot-cost accounting.
- Subgroup tabs that filter presentation without redefining Tab/Shift+Tab selection semantics.
- Direct unit-card selection and Shift-toggle through public `Game.select()`.
- Deterministic application-composition installation, mission cleanup, and exact reverse disposal.
- Dedicated styling, ownership documentation, and focused deterministic coverage.

## Self-review

Final review corrected three semantic defects before completion:

- numeric stable IDs now sort numerically instead of lexically;
- subgroup-tab rerenders preserve the existing presentation model instead of treating it as entity input;
- transport and garrison usage sums authoritative slot costs instead of counting contained records.

No further correctness defect, review comment, or unresolved review thread remained after the corrected workflow passed. GitHub reported the PR cleanly mergeable against latest `main`; the final squash merge is therefore performed by the server against the current base rather than preserving branch merge history.

## Verification

Focused exact-source verification passed:

```text
node --check src/ui/selection-panel-model.js
node --check src/ui/selection-panel.js
node --check tests/ui/selection-panel.test.mjs
node --test tests/ui/selection-panel.test.mjs
```

Result: 5 passed, 0 failed.

Authoritative GitHub Actions run `30880791067` on corrected implementation head `27e75fb6bbdb7e76428b2b76baa50985919d6d44` passed:

- full assembled verifier;
- active-claim diagnostics;
- browser startup and first-mission smoke;
- completion-evidence audit;
- verification diagnostics upload.

A marker-inclusive workflow is required before merge and is recorded in PR #137.

## Evidence limits

The panel is installed through deterministic application composition and the assembled browser starts successfully, so this record claims `RUNTIME_INTEGRATED`. No complete manual visual capture set or player interaction sign-off was performed, so it does not claim `PLAYER_VERIFIED` or `RELEASE_VERIFIED`.

## Scope

No combat or economy rules, selection semantics, transport or garrison mechanics, balance values, audio or art assets, campaign content, broad HUD redesign, or sibling-game behavior changed.
