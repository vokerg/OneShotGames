# UFR-052 — Add idle-worker controls and worker task visibility

- PR: #68
- Completed commit: pending maintainer squash merge
- Acceptance:
  - Adds a compact top-bar worker overview with idle, gathering, returning, building, and other task counts.
  - Adds metal, fuel, and intel worker assignment counts plus currently carried totals.
  - Adds stable select-by-task and select-by-resource controls, preserving the current selection for empty filters.
  - Adds the configurable `selectIdleWorker` action on `I`; `Shift+I` selects all idle workers.
  - Adds selected-worker task and `carry/40` presentation without changing authoritative worker simulation.
- Verification:
  - `node --check src/input/action-map.js`
  - `node --check src/input/worker-overview.js`
  - `node --check src/main.js`
  - `node --check tests/input/worker-overview.test.mjs`
  - `node --test tests/input/worker-overview.test.mjs` — 7 passed, 0 failed
  - Exact connector-visible blobs matched the locally verified Git object hashes for the action map, worker adapter, runtime composition, and test fixture.
  - Connector-visible scope review confirms all changes remain under `ukrainian-front-rts/`.
- Notes:
  - Reconciled with merged UFR-027 tactical-command actions/runtime and UFR-045 veterancy controller/indicator composition.
  - Does not change gathering rates, resource policy, construction behavior, production, combat, campaign state, `src/ui.js`, renderer art, or sibling games.
  - Native `bash verify.sh`, `./run.sh`, and browser mission playtesting were unavailable because the connector-only environment cannot resolve `github.com`; focused reconstructed verification was used.
