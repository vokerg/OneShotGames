# UFR-133 — Define UI architecture

- PR: #50
- Completed commit: pending maintainer merge
- Base: `1fb660c273acc3011673d6ed119762e34836b6ff`
- Verification: `node --check src/ui/ui-contract.js`; `node --check src/ui/ui-state.js`; `node --check tests/ui/ui-state.test.mjs`; `node --check scripts/lib/architecture-verifier.mjs`; `node --check tests/tooling/architecture-verifier.test.mjs`; `node --test tests/ui/ui-state.test.mjs tests/tooling/architecture-verifier.test.mjs` (21 passed, 0 failed). Exact connector-visible blobs matched the locally verified Git object hashes: `412f39b1598dfc68f188bf7edc522607bce145b9` (`src/ui/ui-contract.js`), `bcff6f562441d82b4be3e657c01742d5316d22a8` (`src/ui/ui-state.js`), `06c6677f675438a237dc31f1a7cd76d581ece7d1` (`tests/ui/ui-state.test.mjs`), `de92d1df70b08a892ea946027212074d0711f3a5` (`scripts/lib/architecture-verifier.mjs`), and `6ac618a1d701032d42deb639e20bbcfae65ae241` (`tests/tooling/architecture-verifier.test.mjs`). The branch was zero commits behind current `main` at completion. Native `bash verify.sh` could not run because the connector environment exposes no repository checkout and the local execution environment cannot resolve `github.com`; no GitHub Actions run or commit status check was configured for the PR head. Browser playtesting is not applicable because this task adds browser-independent contracts, tests, verification rules, and documentation without wiring or changing the current runtime UI.
- Acceptance: Adds a validated base/overlay/modal screen registry; canonical HUD and refresh regions with named component owners; derived gameplay/UI/modal input capture policy; deterministic semantic focus restoration and recovery; immutable JSON-like presentation snapshots; stable equality suppression; batched region invalidation and refresh plans; executable architecture classification for dedicated `src/ui/` modules; and focused extension/ownership documentation.
- Scope: Existing `src/ui.js`, markup, styles, gameplay commands, authoritative simulation state, rendering, production, campaign state, balance, and sibling games are unchanged. Later UI tasks may migrate browser components onto this contract incrementally.
