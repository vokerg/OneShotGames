# BUG-303 — Harden live-locale browser teardown

- Issue: #303
- PR: #304
- Completion evidence: **RUNTIME_INTEGRATED**
- Implementation head: `dbd62736784bed715b9453bca7510f9c718c6370`

## Delivered

- Reused the shared bounded Chromium shutdown contract for DevTools sessions.
- Blocked Chrome profile deletion until Chromium exit is confirmed.
- Added bounded retry/backoff for transient `ENOTEMPTY`, `EBUSY`, and `EPERM` profile-cleanup failures.
- Persisted structured live-locale teardown diagnostics without weakening localization assertions.
- Preserved the primary localization/browser error and aggregates teardown failures when both occur.
- Added focused cleanup-policy tests and a live-locale teardown composition regression.
- Left localization copy, gameplay, simulation, and UI behavior unchanged.

## Verification

GitHub Actions `Ukrainian Front RTS verification` run `35471156610` passed **twice** on exact implementation head `dbd62736784bed715b9453bca7510f9c718c6370`.

Both attempts passed the full assembled verifier and the Browser localization smoke, including `scripts/live-locale-browser-smoke.mjs`. The first successful run's diagnostic artifact recorded zero browser errors, graceful Chromium exit, confirmed profile removal, a closed HTTP server, and zero teardown errors.

Additional headed release-browser QA on the implementation head passed Safari and Firefox during closeout.

## Scope guard

Tooling/browser-smoke only. No localization content, gameplay, simulation, UI behavior, balance, campaign content, assets, or sibling-game behavior changed.
