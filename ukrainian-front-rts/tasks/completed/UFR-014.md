# UFR-014 — Add numbered control groups

- PR: #20
- Completed commit: pending maintainer squash merge
- Verification: focused local mirror checks passed with `node --check src/input/control-groups.js` and `node --test tests/input/control-groups.test.mjs`; coverage includes Ctrl+1–9 assignment, Shift/Ctrl+Shift additive membership, number-key recall, shifted `DigitN` decoding, friendly-unit filtering, duplicate suppression, deterministic removal of destroyed/invalid members, and double-tap camera centering. Connector diff review confirms changes are restricted to `ukrainian-front-rts/` and avoid UFR-007's `src/game.js` simulation hotspot. A native full-checkout `bash verify.sh`, `./run.sh`, and browser playtest could not be executed because the execution environment cannot resolve `github.com`; the draft PR remains unmerged for maintainer/CI verification.
- Notes: Adds an input-layer control-group controller with slots 1–9. Ctrl+number replaces a group, Shift+number or Ctrl+Shift+number adds selected Ukrainian units, number recalls, and a second recall within 350 ms focuses the camera on the group centroid. Membership is cleaned against the live friendly-unit roster before each operation. No simulation, balance, content, renderer, UI-layout, save, or replay behavior changes.
