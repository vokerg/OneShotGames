# UFR-015 — Add selection subgroups and stable primary entity rules

- PR: #21
- Completed commit: pending maintainer squash merge
- Verification: focused local mirror passed `node --check src/input/selection-subgroups.js` and `node --test tests/input/selection-subgroups.test.mjs` (4/4 tests). Coverage includes stable primary fallback, explicit primary retention, deterministic Tab/Shift+Tab subgroup cycling, on-screen camera filtering, friendly/living/type filtering, and primary entity resolution. Full `bash verify.sh`, browser playtest, and native checkout remain unavailable because this environment cannot resolve `github.com` outside the GitHub connector.
- Notes: Adds a focused selection subgroup controller. Tab cycles unit-type subgroups, Shift+Tab cycles in reverse, Ctrl+click selects all living friendly units of the clicked type currently visible in the viewport, and mixed selections expose an explicit stable primary entity to existing HUD consumers. No simulation lifecycle, movement, combat, balance, content, save, replay, renderer, or sibling-game code is changed.
