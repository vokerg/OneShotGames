# BUG-279 — Paired vehicle mission readability teardown race

Status: DONE
Completion evidence: RUNTIME_INTEGRATED
PR: #283 (supersedes draft #280 due connector Ready-for-review failure)

## Delivered

- Replaced the paired-vehicle smoke's teardown race with a reusable bounded child-process shutdown helper that waits independently after SIGTERM and SIGKILL.
- Added deterministic regression coverage for already-exited, graceful SIGTERM, forced SIGKILL, and genuinely stuck process paths.
- Preserved all eight Ukrainian/Russian vehicle mission captures, atlas readiness checks, runtime/network failure checks, viewport placement checks, and screenshot-size assertions.
- Added structured teardown diagnostics for process signals/waits, DevTools socket state, HTTP server close, temporary profile removal, and phase-specific cleanup errors.
- Clear the dedicated paired-vehicle artifact directory before each run so stale pass evidence cannot survive a later early failure.

## Verification

Implementation head `671d405494266c41ece053b4d67a12e9e1126750` was exercised twice on the same SHA in GitHub Actions workflow run `33594677208`:

- PASS — full assembled verifier (`bash verify.sh`) and the four-case bounded-process shutdown regression suite.
- PASS — paired Ukrainian/Russian vehicle actual-mission readability review, attempt 1 (~10s), with 8/8 captures and both atlases ready/non-degraded.
- PASS — the same paired-vehicle review on workflow attempt 2, unchanged SHA (~10s).
- PASS — both attempt artifacts report teardown `status: passed`, graceful SIGTERM exit, HTTP server closed, profile removed, and zero teardown errors.

The only failing workflow step on that historical base was the independent visual-regression timeout tracked as BUG-247 / PR #278; no visual-regression files are changed by BUG-279.

## Self-review

The shutdown helper is fail-closed: if Chromium ignores both signals and remains alive after the bounded waits, it throws `ProcessShutdownError` with signal/wait diagnostics. The smoke still treats genuine capture/runtime/readability errors as authoritative and does not weaken any visual assertion.

No gameplay, renderer, mission, balance, art, or runtime behavior changed.

## Merge authorization / temporary required-check waiver

On 2026-09-02 the maintainer explicitly instructed a final self-review and squash merge if the changes are good. The remaining red global verification step is the separately diagnosed BUG-247 visual-regression flake, already fixed and repeatedly verified in PR #278. This completion therefore records that explicit maintainer authorization while retaining the blocking follow-up reference rather than misrepresenting the unrelated check as green.
