# UFR-102 — Campaign finale integration

- Final PR: #228
- Completion evidence: **RUNTIME_INTEGRATED**
- Base: `4b84ad342a50b232a3132b20ca7dd6ac3cc4d94d`

## Delivered

- a deterministic nine-operation campaign registry that preserves the authored UFR-094 through UFR-101 sequence and terminates in one finale operation;
- a multi-stage finale that consumes prior campaign choices, upgrades, difficulty, and operation performance instead of introducing an isolated mission state;
- full Ukrainian and Russian runtime roster composition in the finale, with authored objective stages and adaptive AI pressure;
- ordered operation unlock/progression, mission result recording, final debrief, and a victorious terminal transition into credits;
- campaign progression installation through the existing menu/UI composition with profile synchronization and a browser diagnostic surface;
- focused regression coverage for operation ordering, roster completeness, deterministic callbacks, unlock progression, finale victory/credits, and browser installation lifecycle.

## Verification

Exact implementation head workflow `31426438836` on `5b2ab5eb600c46e203264502cbf7ac7b0d9b4fc4` passed the full assembled verifier and all repository browser/UI/rendering/accessibility/localization/evidence gates.

Self-review caught an initial architecture-boundary violation: campaign progression had been placed under `src/app/` while owning browser diagnostics, and `src/ui/menu-stack-composition.js` imported that app module in a forbidden `ui -> app` direction. The implementation was refactored before closeout so progression/browser composition lives in the UI layer, eliminating both violations; the green exact-head workflow above verifies the corrected structure.

The completion level is `RUNTIME_INTEGRATED`: the campaign progression/finale controller is installed by the live menu composition and synchronizes the campaign profile used by the game runtime.

The closeout commit is followed by one final exact-head workflow before squash merge.
