# UFR-083 — Add skirmish framework

- Final PR: #195
- Completion evidence: **PLAYER_VERIFIED**
- Implementation branch: `ufrts/UFR-083-skirmish-framework`
- Revalidated after the shared verification/localization baseline fix (#198) and Ukrainian infantry faction/side rendering integration (#188/#205).

## Delivered

- deterministic custom skirmish setup with authored battlefield, player faction, opponent, and fair AI difficulty selection;
- three authored balanced battlefields with explicit starts, roads, terrain seeds, finite shared resources, and symmetric starting wallets;
- Ukraine-as-player and Russia-as-player support with gameplay side ownership kept separate from national faction identity;
- campaign wave spawning disabled and victory/defeat routed through the existing authoritative objective/outcome system;
- AI economy workers physically gather, carry, return, and deposit finite shared resources rather than receiving remote income;
- AI production respects authoritative unit population costs, queued population, and building-derived command capacity;
- tactical reaction windows cannot be bypassed by legacy Russian auto-acquisition, and economy workers are excluded from combat auto-acquisition;
- setup, retry/return, status, production, and result presentation through public game commands;
- skirmish runtime/start lifecycle composes through the final assembled `game.start()` wrapper chain;
- `src/skirmish/` architecture ownership, UI-safe declarative catalog boundary, and accepted/rejected verifier fixtures documented and enforced.

## Verification

Exact-head integration run `31239739261` on `96fea8c6f1f7960005c01480085efff2b5801f7d` passed the complete Ukrainian Front RTS workflow after the architecture corrections, including:

- full assembled verifier;
- all five focused skirmish framework tests;
- skirmish architecture accepted/rejected tooling fixtures;
- active-claim diagnostics;
- browser startup and mission smoke;
- Ukrainian infantry Art Lab browser review from the then-current shared baseline;
- visual regression capture;
- browser audio autoplay/resume smoke;
- accessibility/focus-pause smoke;
- browser localization smoke;
- completion evidence audit.

Earlier focused self-review also directly covered physical gather/carry/drop-off, command-capacity parity, Russia-as-player ownership, HQ victory semantics, and late-installed `game.start()` reset composition.

The conveyor closeout documentation/completion commits are followed by one final exact-head workflow before merge; the PR is not merged unless that final run remains green against current `main`.
