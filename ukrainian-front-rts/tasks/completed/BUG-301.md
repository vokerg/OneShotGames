# BUG-301 — Harden tactical-alert browser teardown

- Issue: #301
- PR: #302
- Completion evidence: **RUNTIME_INTEGRATED**
- Implementation head: `11f5b77b1e9be30082db78fd09e7affdd878ff9e`

## Delivered

- Reused the shared bounded Chromium shutdown contract in the tactical-alert browser smoke.
- Fail closed when Chromium survives both the SIGTERM and SIGKILL windows instead of deleting a profile that may still be live.
- Retry transient `ENOTEMPTY`, `EBUSY`, and `EPERM` profile-removal failures.
- Persist structured `teardown.json` evidence on success and `teardown-failure.json` diagnostics on teardown failure.
- Preserve the primary browser/test error and aggregate it with teardown errors when both occur.
- Added focused tooling coverage proving the tactical-alert smoke keeps profile cleanup behind confirmed Chromium exit.
- Left all sustained tactical-alert product assertions unchanged.

## Verification

GitHub Actions `Ukrainian Front RTS verification` run `35462948144` on implementation head `11f5b77b1e9be30082db78fd09e7affdd878ff9e`: **PASS**.

That exact-head run passed the full assembled verifier, active-claim diagnostics, browser startup/mission smoke, the hardened sustained tactical-alert browser regression, compact viewport, construction, command-card, infantry/vehicle readability, visual-regression, audio, accessibility, localization, completion-evidence audit, and diagnostic upload.

The exact completion-bookkeeping head is verified separately in PR #302 before review readiness.

## Scope guard

Tooling/browser-smoke only. No simulation, gameplay, UI behavior, balance, campaign content, assets, or sibling-game behavior changed.
