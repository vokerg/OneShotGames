# BUG-248 — Actual-mission infantry readability smoke intermittency

Status: DONE
Completion evidence: RUNTIME_INTEGRATED
PR: #257

## Delivered

- Characterized the intermittent actual-mission infantry readability failure as a teardown false-negative rather than a deterministic visual/readability failure: the historical failed run had already produced the success manifest and all eight expected mission captures before the process failed during browser/profile cleanup.
- Added persistent phase, scene, capture, browser-exit, and cleanup diagnostics under `artifacts/` so future failures identify the verification phase without depending on ephemeral Actions logs.
- Hardened Chrome teardown with bounded SIGTERM/SIGKILL waits and explicit exit tracking, retried transient profile-removal races, and separated cleanup warnings from the primary verification verdict.
- Kept the existing mission/readability assertions, capture set, visual thresholds, and fail-closed primary-error behavior unchanged.

## Verification

Implementation head `d61bf8b3d8546c5e65d9db4951b3318921868819` ran GitHub Actions workflow `31938645149` (run #1450) twice on the unchanged SHA:

- PASS (attempt 1) — full assembled verifier, active claim diagnostics, browser startup/mission smoke, command-card real-HUD review, Ukrainian infantry Art Lab review, **Ukrainian infantry actual-mission readability review**, paired vehicle actual-mission readability review, browser audio, accessibility/focus-pause, localization, completion-evidence audit, and artifact upload.
- PASS (attempt 2) — the same BUG-248 actual-mission infantry readability review passed again on the unchanged implementation SHA, alongside the assembled verifier and adjacent browser checks.
- The separate `Visual regression review capture` step remained intermittent/failing and is explicitly tracked by open bug #247. BUG-248 does not modify `scripts/visual-regression-smoke.mjs` or weaken that gate.

The repeated unchanged-head BUG-248 passes satisfy issue #248's acceptance requirement to demonstrate deterministic execution after the teardown fix. The persistent diagnostic contract also distinguishes primary verification failure from post-verification cleanup warnings.

## Self-review

The final review confirmed that a genuine readability/runtime error remains authoritative: the `catch` records and rethrows the original exception, while `finally` only captures teardown failures and writes diagnostics. Cleanup errors therefore cannot mask a primary failure. Conversely, after `verificationPassed` is true, a transient teardown/profile-removal error is reported as a cleanup warning instead of retroactively failing a completed mission/readability verification.

No renderer, gameplay, mission data, asset, balance, or visual-threshold code changed. The only non-green workflow step is the unrelated known visual-regression flake #247, which remains independently tracked and outside this branch's file ownership.