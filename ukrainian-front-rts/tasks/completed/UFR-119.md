# UFR-119 — Produce portraits, icons, cursors, pings, and medals

- Pull request: #144
- Branch: `ufrts/UFR-119-portraits-icons`
- Original base: `2166e9a8af0636f8c1b4fcc73b9190d67f7b728c`
- Synchronized implementation head: `f188c10fa6453ddff386870ed7cc2deccad1158b`
- Evidence level: `CONTRACT_COMPLETE`

## Delivered

- Added a deeply immutable, versioned UI-art catalog covering 100 assets across 10 families.
- Covered all 16 current fictionalized runtime-unit portraits and unit icons, four buildings, 14 abilities, six upgrades, and 11 objective types.
- Added stable command/targeting cursor, minimap/alert ping, reusable medal, and visible missing-asset fallback vocabularies.
- Declared production dimensions, portrait safe areas, 1×/2× cursor coverage, bounded hotspots, ping duration, and static reduced-motion alternatives.
- Added original repository-authored deterministic SVG recipes, a provenance/source manifest, a build command, runtime-manifest generation, symbol-sheet generation, and contact-sheet generation.
- Added exact runtime lookup and fallback helpers without changing UI layout, command handling, objectives, progression, scoring, or simulation ownership.
- Registered the UI-art verifier in the authoritative verification plan and documented downstream ownership for UI skin, campaign art, battlefield atlases, and visual regression.

## Self-review

The complete task diff was reviewed for acceptance coverage, canonical ID drift, dependency direction, generated-output ownership, deterministic behavior, palette and dimension contracts, accessibility metadata, provenance, public-figure exclusion, fallback behavior, scope, and documentation.

Corrections made during self-review:

- Removed a temporary checked-in generated runtime manifest so source authority remains the catalog plus provenance manifest rather than a parallel generated database.
- Changed CI verification to regenerate the symbol sheet, runtime manifest, and contact sheet in memory without mutating the checkout.
- Added exact symbol/contact-sheet coverage checks for every catalog entry and explicit rejection of embedded text.
- Kept the player-facing integration boundary with UFR-120/UFR-122 instead of modifying active UI layout or claiming broad visual adoption.

No blocking defect remained after review. PR discussion contained no comments, submitted reviews, or unresolved inline threads before completion.

## Verification

Synchronized implementation head `f188c10fa6453ddff386870ed7cc2deccad1158b` passed GitHub Actions run `30883987388`:

- 915 Node tests passed; 0 failed;
- 352 ordered verifier stages passed;
- 100 UI-art assets across 10 families verified;
- deterministic 23,704-byte SVG symbol sheet and 26,454-byte contact sheet reproduced in memory;
- active-claim diagnostics passed;
- browser startup and first-mission smoke passed with a valid canvas and three mission cards;
- browser warnings: none;
- completion-evidence audit and diagnostics upload passed;
- branch was zero commits behind `main` at verification.

## Runtime composition evidence

The catalog and deterministic generation/lookup contracts are production-ready and consumable from the UI layer. UFR-119 does not install a second UI composition path or alter authoritative gameplay state. Broad UI mounting and skin adoption remain with UFR-120 and later UI tasks.

## Browser/player evidence

Automated browser smoke proves the assembled application still starts the first mission successfully after the new source, verification, and UI contract modules are present. It does not constitute a human visual review of all 100 generated assets in every UI location, scale, grayscale mode, or color-vision preset.

## Save implications

No save, checkpoint, replay, profile, or migration schema changes are introduced. UI-art state is static presentation metadata.

## AI-system use

No AI planner, observation, doctrine, budget, or command behavior is changed. AI systems may remain unaware of presentation assets.

## Evidence boundary

`CONTRACT_COMPLETE` is the highest justified level. The complete source/runtime asset family, provenance, deterministic generation, lookup, fallback, and verification contracts exist, but the prototype UI is not broadly reskinned and no manual visual matrix was performed. `RUNTIME_INTEGRATED` and `PLAYER_VERIFIED` are therefore not claimed.
