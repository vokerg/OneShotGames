# UFR-122 Completion Evidence

- Task: `UFR-122 — Add visual-regression scenes`
- Final implementation commit: `e13e2db0b32dc0451a440c133fdcf5336cf7346e`
- PR: `#166`
- Evidence level: `CONTRACT_COMPLETE`
- Verifier:
  - GitHub Actions run `30982605897` passed on the exact implementation head.
  - Full assembled verifier, architecture verification, active-claim diagnostics, completion-evidence audit, and existing browser startup/mission smoke all passed.
  - `node scripts/verify-visual-regression.mjs` validated deterministic canonical coverage.
  - `node scripts/visual-regression-smoke.mjs` rendered the browser fixture, confirmed its ready marker and scene count, captured the review image, and published it through the standard verification artifact.
- Artifact paths:
  - `visual-regression.html`
  - `visual-regression.css`
  - `src/render/visual-regression-scenes.js`
  - `src/render/visual-regression.js`
  - `scripts/verify-visual-regression.mjs`
  - `scripts/visual-regression-smoke.mjs`
  - `docs/VISUAL_REGRESSION.md`
  - `artifacts/visual-regression/visual-regression-overview.png` (CI artifact)
  - `artifacts/visual-regression/visual-regression-manifest.json` (CI artifact)
- Acceptance evidence:
  - The generated catalog covers every canonical unit type for both Ukraine and Russia, every faction-specific building atlas identity, every terrain visual family in every terrain biome profile, and every effects-atlas family.
  - Dedicated scenes cover all declared production screen identities, overview/standard/detail zooms, full color, grayscale, protanopia, deuteranopia, and tritanopia review modes.
  - Stable `category:identity` scene IDs, deterministic ordering, uniqueness, authoritative-export reconciliation, browser readiness, rendered scene count, and screenshot generation are verified.
  - CI uploads the 1920×1080 review capture and machine-readable manifest with the standard Ukrainian Front RTS diagnostics for pre-merge art review.
- Residual runtime scope:
  - This is presentation review tooling, not a pixel-baseline gate; human reviewers still decide whether a captured art change is acceptable.
  - Browser rasterization is intentionally not compared byte-for-byte across platforms, while scene identity and coverage remain deterministic contracts.
- Notes:
  - The implementation stays in the render/tooling boundary and does not change simulation, campaign state, persistence, production UI ownership, atlas generation, localization, audio, balance, or sibling games.
