# UFR-002 — Add a task-queue validator

- PR: #11
- Completed commit: pending maintainer squash merge
- Verification: `node --check scripts/verify-task-queue.mjs`; `node --check scripts/verify-task-queue.test.mjs`; `node scripts/verify-task-queue.test.mjs` (`Task queue validator tests passed (7 cases).`); `node scripts/verify-task-queue.mjs TASKS.md` against the exact connector-fetched current queue (`Task queue verification passed for 168 tasks.`); an intentionally invalid queue exited 1 with actionable dependency and parallel-policy errors; `bash -n verify.sh` passed. The connector environment does not expose a full repository checkout, so end-to-end `bash verify.sh` could not be executed locally; the unchanged base commit's existing source syntax and architecture checks were already recorded as passing in UFR-001's completion evidence. No browser playtest is applicable to this tooling-only change.
- Notes: Adds a dependency-free Markdown queue parser/validator for required fields, `UFR-###` IDs, P0–P3 priorities, unique and monotonically increasing IDs, explicit `YES`/`LIMITED`/`NO` parallel policies with explanations, dependency references, dependency ranges, descending ranges, and self-dependencies. Integrates validator fixtures and the real queue check into `verify.sh`, while replacing Bash process substitution with a pipe-safe file walk. No runtime, content schema, balance, visual, audio, or asset implications.
