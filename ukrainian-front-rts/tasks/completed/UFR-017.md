# UFR-017 — Add queued orders and deterministic execution

- PR: #26
- Completed commit: pending maintainer squash merge
- Verification: focused local mirror passed `node --check src/input/queued-orders.js` and `node --test tests/input/queued-orders.test.mjs` with 6/6 tests passing.
- Coverage: normal order replacement, Shift-appended queues, deterministic FIFO advancement, live waypoint data for movement and attack targets, controller integration, and Stop clearing active and pending commands.
- Notes: installs a focused queued-order controller at the runtime boundary, preserving the existing fixed-step simulation phases and current `Game.issue`, `Game.updateUnit`, and `Game.stopSelected` behavior. The controller exposes `game.queuedWaypoints(unit)` for renderer/UI consumers. Full checkout verification and browser playtesting remain unavailable in the connector-only environment.
