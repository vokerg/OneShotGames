# UFR-021 — Add footprint-aware collision and deterministic overlap resolution

- PR: #33
- Completed commit: a27288ee14f7012cd0828ee9afed6698fd3950ca
- Acceptance: Adds size-derived ground-unit collision footprints, mass-weighted soft separation, deterministic exact-overlap directions, stable unit-ID pair ordering, world-edge footprint clamping, and fixed-step integration after all waypoint movement. Air and destroyed units are excluded; collision changes positions only and preserves active route/order state.
- Verification: `node --check src/systems/unit-collision-system.js`; `node --check src/systems/navigation-movement-system.js`; syntax checks for all navigation tests; `node --test tests/navigation/*.test.mjs` in a reconstructed focused fixture using the exact connector-visible navigation/collision/movement modules and connector-fetched runtime constants (36 tests passed, 0 failed).
- Performance: A dense 150-unit mixed infantry/vehicle collision fixture across 100 fresh runs completed in 172.290 ms total, averaging 1.723 ms per resolution and 3,098 resolved pair-pass interactions per run.
- Verification limitations: The connector environment provides no repository checkout and the local container cannot resolve `github.com`, so native `bash verify.sh`, `./run.sh`, and browser mission playtesting could not be executed. Focused reconstruction and connector-visible diff review were used instead.
- Documentation: `docs/NAVIGATION.md` records fixed-step sequencing, footprint derivation, mass weighting, bounded soft-separation passes, deterministic ordering, diagnostics, and subsystem ownership boundaries.
