# UFR-117 — Add environment props and destruction variants

- Evidence level: `CONTRACT_COMPLETE`
- Dependency: UFR-116
- Parallel boundary: prop atlas separate from core terrain

## Delivered

- Immutable versioned environment-prop presentation contract.
- Shelterbelt, tree, wall, fence, house, industrial, crater, and wreckage families.
- Stable aliases, footprints, canvases, layers, lifecycle states, seasons, and deterministic variants.
- Donbas, Zaporizhzhia, and Kherson palette profiles.
- Reference-free UFR-088 authored-prop projection preserving cell, footprint, blocking layers, and metadata.
- UFR-044/UFR-057-compatible state projection without duplicate lifecycle rules.
- Stable layer/depth/x/id ordering and duplicate-ID rejection.
- Deterministic fade/cutaway/outline policy for every tall occluder.
- Visible diagnostic fallback for missing type or season coverage.
- Original CC0 deterministic vector-atlas generator and write/check command.
- Complete 730-frame UFR-107-compatible manifest coverage with anchors, masks, tags, and provenance.
- Focused tests and implementation documentation.

## Self-review corrections

- Ensured cleared wreckage intentionally emits no frame rather than an invisible missing asset.
- Kept authored footprint and blocking layers independent from visual silhouette.
- Made unsupported coverage visibly diagnostic instead of silently substituting unrelated art.
- Required explicit fade/cutaway metadata for every tall occluder.
- Put deterministic variation marks above opaque family geometry so variants are visibly distinct.
- Kept generated atlas outputs reproducible build products rather than undocumented hand-edited assets.

## Verification

Focused local fixture:

```text
node --check src/render/environment-prop-system.js
node --check scripts/lib/environment-prop-atlas-generator.mjs
node --check scripts/build-environment-prop-atlas.mjs
node --check tests/art/environment-prop-system.test.mjs
node --test tests/art/environment-prop-system.test.mjs
```

Result: **9 passed, 0 failed**.

The final authoritative GitHub Actions run and exact head are recorded in PR #145 after the
marker-inclusive branch passes the full assembled verifier, active-claim diagnostics, browser smoke,
completion-evidence audit, and diagnostics upload.

## Evidence ceiling

The contract, original atlas generator, family/state/season coverage, projection, layering, fallback,
and tests are complete. The active browser runtime does not yet materialize authored-map props or
load this atlas, so this marker does not claim `RUNTIME_INTEGRATED`, `PLAYER_VERIFIED`, art-lab
captures, or renderer performance closure.
