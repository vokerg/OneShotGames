# BUG-272 — Remove mixed English/Ukrainian strings from the live Ukrainian locale

- Issue: #272
- PR: #290
- Completion evidence: **PLAYER_VERIFIED**

## Delivered

- Localized mission cards and active mission title, story, region, and objectives with reversible `en → uk → en` presentation.
- Localized tactical/stance command cards, including disabled reasons and dynamic command feedback.
- Localized runtime-owned pause/settings, notification/minimap, skirmish, toast, and authored campaign selector/briefing surfaces through a reversible presentation bridge.
- Added active browser coverage for locale switching on both the operation selector and an active authored mission.
- Preserved simulation/save ownership: the compatibility bridge mutates presentation only and restores the original English DOM on locale reversal/disposal.
- Updated the command-card verifier to recognize localized attack-ground semantics by stable command ID/localization key/runtime command rather than a literal title field.

## Verification

- `Ukrainian Front RTS verification` run 1642, head `ab0212ac09816c00e87af933bf4da84777a6ba65`: **Full assembled verifier passed**.
- Same run: browser startup/mission smoke passed.
- `Ukrainian Front RTS verification` run 1641, product head `58b856d1676da0d8d1f0872d9ef999013c297f4f`: **Browser localization smoke passed**, including `browser-localization-smoke.mjs` and the new `live-locale-browser-smoke.mjs` (`en → uk → en` on selector and active mission).
- The only change from product head `58b856d...` to verifier head `ab0212a...` is `scripts/verify-command-card.mjs`; runtime/localization code is unchanged.
- Command-card real-HUD readability, Ukrainian infantry Art Lab, visual regression, audio release, and accessibility browser smokes passed on the product verification run.

## Baseline exceptions

The verification workflow remains red overall because these browser regressions already fail on current `main` (`5505fd643c6b84a487efcc2adf9251be63f04e3d`) with the same pattern:

- Engineer subgroup construction browser regression.
- Ukrainian infantry actual-mission readability review.
- Paired Ukrainian/Russian vehicle actual-mission readability review.

On that `main` run, the full assembled verifier and browser localization smoke pass. These failures are therefore pre-existing and are not introduced by BUG-272; they do not raise BUG-272 to RELEASE_VERIFIED.
