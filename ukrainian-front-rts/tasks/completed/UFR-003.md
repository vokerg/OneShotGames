# UFR-003 — Define versioned content schemas

- PR: #12
- Completed commit: pending maintainer squash merge
- Verification: `node --check src/content-schema.js`; `node --check scripts/verify-content-schema.mjs`; `node scripts/verify-content-schema.mjs` (`Content schema verification passed for 8 families.`); `node scripts/verify-task-queue.test.mjs` (`Task queue validator tests passed (7 cases).`); `node scripts/verify-task-queue.mjs TASKS.md` (`Task queue verification passed for 168 tasks.`); `bash -n verify.sh`; whitespace checks over all changed text files. Connector-fetched blob SHAs matched the exact locally verified schema, verifier, documentation, architecture, change-guide, and `verify.sh` files. The new source module has no imports, so it introduces no higher-layer dependency. End-to-end `bash verify.sh` could not be run because the connector environment does not expose a full checkout and the local runner cannot resolve `github.com`; the unchanged base architecture check was previously recorded as passing for 14 modules. Browser playtesting is not applicable to this schema/documentation/tooling-only task.
- Notes: Introduces content schema version 1 for factions, units, buildings, abilities, upgrades, missions, maps, and AI profiles. Each contract defines collection/identity rules, required fields, explicit defaults, references, numeric constraints, and additive-extension policy. Adds cloned default materialization and a dependency-free contract verifier, documents breaking/additive version rules and shared payload types, and updates architecture/change-routing ownership. Existing runtime content and behavior are unchanged; map loading, AI behavior, migrations, and full cross-record validation remain assigned to later queue tasks.
