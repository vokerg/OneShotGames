# BUG-273 — Keep operation-selector utility controls reachable

- Issue: #273
- PR: #291
- Completion evidence: **PLAYER_VERIFIED**

## Delivered

- Keep the operation-selector top bar above the selector layer while allowing pointer events only on valid selector utilities.
- Keep language, audio/accessibility, help, and fullscreen controls visible, enabled, keyboard reachable, and pointer reachable while the selector is active.
- Hide battlefield-only top-bar content while the selector is active and restore normal top-bar interaction after entering an authored operation.
- Add real-browser hit-target plus pointer/keyboard activation coverage before operation start and recovery coverage after operation start.
- Exclude intentionally hidden selector-state top-bar children from localization overflow accounting.

## Verification

Behavior head before completion-marker bookkeeping: `bd045e97cf7c75729cf75923a8a5135e1574c687`.

GitHub Actions run `34739321443` (Ukrainian Front RTS verification):
- Full assembled verifier/test suite completed before browser regressions.
- Browser startup and mission smoke: **PASS**, including BUG-273 selector utility hit targets, pointer activation, keyboard activation, selector-only battlefield-control suppression, and post-start utility recovery.
- Browser localization smoke: **PASS**.
- Live locale selector/mission smoke: **PASS**.

GitHub Actions run `34739321463` (headed release browser QA) remains red with the same pre-existing mission-start timeouts already present on current `main`.

## Baseline exceptions

Current `main` (merge commit `ef7debc1618678f7cd37e9ccd9d7cdf0dde864cf`, PR #290) already fails the same unrelated browser surfaces:

- Engineer subgroup construction browser regression: mixed starting-force selection timeout.
- Ukrainian infantry actual-mission readability review: first mission start timeout.
- Paired Ukrainian/Russian vehicle actual-mission readability review: mission start timeout.
- Headed release QA on Chrome, Edge, Firefox, and Safari: mission/fresh-mission timeout.

These failures are outside BUG-273 ownership and are unchanged by this PR. They prevent RELEASE_VERIFIED evidence but do not invalidate the BUG-273 player-flow acceptance path.

## Self-review

- Scope stays inside selector/top-bar presentation and focused browser coverage.
- No simulation, balance, campaign-content, or sibling-game behavior changes.
- Post-start assertions respect responsive runtime ownership instead of requiring economy/tech controls to be visibly expanded at every viewport.
