# UFR-105 — Campaign alpha gate

Status: DONE

## Delivered

- Added an executable end-to-end alpha gate for all nine campaign operations on Story, Standard, and Veteran (27 total operation traversals).
- Closed a P1 integration blocker by applying the active campaign difficulty balance in the production `beginOperation()` path, including the dynamically assembled finale mission.
- Exercised the canonical mission checkpoint service for every operation/difficulty traversal, including compatibility restores and latest-checkpoint selection.
- Exercised campaign save/restore with active checkpoint-backed mission state for every operation/difficulty traversal.
- Verified strict operation unlock order, zero content-policy violations, no hidden combat-stat difficulty modifiers, finale debrief, `credits-ready`, and credits transition.
- Published `docs/campaign-alpha-readiness.md` and the reusable `scripts/audit-campaign-alpha.mjs` gate.

## Verification

Exact implementation head `81ddc0943347511f5407f53a2cc9ca37516ba976` ran workflow `31479451572`. The UFR-105 alpha regression passed: 27 operation traversals, 27 canonical checkpoint captures/restores, 27 campaign save/restores, three finale-to-credits transitions, and zero content audit violations. Every browser verification step in the workflow passed.

The assembled verifier had one unrelated existing failure: `150 mixed units cross authored chokepoints deterministically within navigation budgets`, where initial 150-unit routing took `5495.45ms`. The UFR-105 alpha test itself passed (`campaign alpha gate completes every operation on every difficulty with saves, checkpoints, and credits`). No navigation thresholds or unrelated navigation code were changed.

## Self-review

The alpha harness uses production progression, balance, checkpoint, save, and content-audit contracts rather than parallel test-only schemas. The initial generic checkpoint-policy assumption was removed after CI exposed that authored mission checkpoint-policy representations are intentionally mission-specific; the corrected gate now validates through the canonical checkpoint service. The runtime balance hook returns transformed copies and does not mutate authored campaign operation constants.