# BUG-244 — Command card text readability

Status: DONE
Completion evidence: RUNTIME_INTEGRATED
PR: #256

## Delivered

- Reworked the production 4×3 command-card presentation into a compact single-row composition with separate title, targeting-state, metadata, and hotkey lanes so constrained HUD rows do not stack text on top of each other.
- Kept full command meaning available through the existing accessible label and an explicit hover/focus detail surface, including programmatic focus used by command-card keyboard navigation.
- Added focused regression coverage for representative production, repair, stance, ability, construction, modernization, disabled, and targeting states, including long English and Ukrainian strings.
- Added a deterministic real-HUD command-card browser fixture and CI smoke at desktop and responsive widths with geometry, full-detail, and screenshot assertions.

## Verification

Implementation/review head `b3d9d612dec5592895908fed6f859c987d3fb328` ran GitHub Actions workflow `31937714766` (run #1445):

- PASS — full assembled verifier.
- PASS — active claim diagnostics.
- PASS — browser startup and mission smoke.
- PASS — command-card real-HUD readability review.
- PASS — Ukrainian infantry Art Lab review.
- PASS — Ukrainian infantry actual-mission readability review.
- PASS — paired vehicle actual-mission readability review.
- PASS — visual-regression review capture.
- PASS — browser audio, accessibility/focus-pause, and localization smokes.
- PASS — completion evidence audit and artifact upload.

The dedicated command-card browser review rendered 8 representative actions with zero text-overlap pairs and zero full-detail failures at 1600px, 1000px, and 760px target widths. The focused full-detail surface resolved to z-index 80 and opacity 1 at every target. The uploaded screenshots for all three viewports were manually inspected during self-review and showed no command-card collisions or hidden focused detail.

## Self-review

The initial review found the PR was not merge-ready because its newly added browser gate was red. Three issues were corrected before completion: Chromium `--dump-dom` did not wait for the fixture's top-level async readiness path; programmatic focus did not reveal the detail surface because CSS only handled `:focus-visible`; and the synchronous check sampled the first frame of the opacity transition. The fixture was made synchronous/deterministic without weakening geometry assertions, the production focus contract was expanded to `:focus`, and the measurement fixture disables only transition timing while sampling final state.

No gameplay or simulation authority changed. Draft release-gate PR #254 currently contains later-loaded command-card presentation overrides in `release-ui.css`; that branch must reconcile this merged BUG-244 contract when it rebases, but it does not block BUG-244 on current `main`.
