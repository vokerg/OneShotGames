# BUG-276 — Aggregate tactical attack alerts and reset them per mission run

- Issue: #276
- PR: #300
- Completion evidence: **RUNTIME_INTEGRATED**

## Delivered

- Aggregated sustained attack damage into one minimap alert per mission run and target, refreshing its position/expiry and incrementing a visible hit count instead of appending repeated rows.
- Aggregated notification-center attack entries across the mission run and bounded unread state to one unread item per aggregated record.
- Prioritized objective and production alerts above repeated attack noise in the minimap queue and notification feed.
- Reset minimap and notification alert state on mission start/restart and return-to-operations transitions, while preserving exact teardown/restoration behavior.
- Added deterministic queue/runtime coverage plus a Chromium sustained-damage regression harness and wired it into the standard RTS verification workflow.

## Verification

Behavior head before completion bookkeeping: `3b8d1fb7520483957b760f7c2c7a24b7bcca08db`.

GitHub Actions `Ukrainian Front RTS verification` run `35452292448`: **PASS**.

The run passed `bash verify.sh`, active-claim diagnostics, browser startup/mission smoke, the new sustained tactical alert browser regression, compact viewport review, construction/readability/visual/audio/accessibility/localization browser checks, and completion-evidence audit.

The sustained-damage smoke verifies five spaced hits collapse to one counted minimap alert and one counted notification record, objective/production priority remains ahead of attack noise, restart starts a fresh count, and return-to-operations clears both alert surfaces.

## Self-review

- Scope is limited to tactical alert aggregation/lifecycle ownership, focused tests, the browser regression harness, CI wiring, and task bookkeeping.
- No simulation, balance, campaign content, assets, or sibling-game behavior changed.
- Current `main` remains `7a88d507fdc6a3bf06774e43f27cd5023eb32fda`; PR #300 is mergeable with no base drift.
