# UFR-145 — Add detailed endgame report and analytics

- PR: #159
- Completed commit: pending maintainer merge
- Base at claim: `aa6efed649af0e78bf2b54317a68a9128e23db65`
- Current pull-request base validated: `81491a2dde5fa030dd9f42ba572f434ede570b83`
- Evidence level: `CONTRACT_COMPLETE`
- Parallel rule: `LIMITED — endgame UI/campaign data`

## Delivered

- Added the deeply immutable, versioned `endgame-analytics-report` contract.
- Added detailed friendly losses/survivors, hostile kills/captures/escapes, damage, healing, repair, and friendly-fire analytics.
- Added conserved economy/resource ledgers, production/construction counts, worker/command peaks, and explicit economy score value.
- Added ordered technology completion/cost, objective timing/status, medals, penalties, timeline, score breakdown, and bounded time bonus contracts.
- Added capability-gated continue, return, retry, save, view-replay, and save-replay action descriptors with disabled reasons.
- Added canonical campaign unlock/upgrade/choice/modifier/persistent-loss consequences.
- Added an exact UFR-084 campaign mission-result handoff and a UFR-089 mission-debrief adapter.
- Added focused validation, determinism, immutability, score, conservation, action, debrief, and campaign-profile regression coverage.
- Documented runtime ownership, score authority, action boundaries, campaign handoffs, and evidence ceiling.

## Self-review correction

The first authoritative run exposed that the UFR-089 debrief contract represents an iconless medal by omitting `iconId`, while the initial adapter passed explicit `null`. The adapter now omits absent icon IDs and preserves iconless medal compatibility. No other test or architecture failure was present.

## Verification

Corrected implementation head: `834bfb31fd4fee2f92e6fa18a24c49be0dd56787`

GitHub Actions `Ukrainian Front RTS verification` run #635 passed:

- full assembled `bash verify.sh`;
- 998 automated tests with zero failures;
- active-claim diagnostics;
- Chromium browser startup and first-mission smoke;
- completion-evidence audit;
- diagnostics upload.

Focused coverage:

- `tests/ui/endgame-analytics.test.mjs` — detailed report, scoring, actions, conservation, canonicalization, immutability, and invalid input;
- `tests/ui/endgame-report-adapter.test.mjs` — UFR-089 debrief compatibility and UFR-084 campaign-profile result recording.

## Evidence boundary

The report and both campaign handoffs are complete contracts, but the active browser endgame screen is intentionally unchanged to avoid overlapping active `src/ui.js`, `src/main.js`, `index.html`, and shared-style owners. Browser smoke demonstrates assembled regression safety rather than player interaction with the unmounted report, so this marker does not claim `RUNTIME_INTEGRATED` or `PLAYER_VERIFIED`.

## Scope

No gameplay, balance, campaign-profile mutation, save/replay implementation, live UI mounting, shared hotspot, asset, audio, localization, or sibling-game behavior changed.
