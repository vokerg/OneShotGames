# UFR-137 — Add economy and production panels

- Pull request: #156
- Branch: `ufrts/UFR-137-economy-production-panels`
- Original base: `860a18ba5fbbedd27cea746c0f4efc60a8326500`
- Final branch policy: clean linear commit on current `main`, zero commits behind immediately before merge
- Evidence level: `RUNTIME_INTEGRATED`

## Delivered

- Extended the existing economy overview into one immutable player-facing economy and production model without changing economy simulation or balance.
- Added delivered income rates, worker totals, task allocation, resource assignments, and carried-resource summaries.
- Added one deterministic global queue spanning production and research, including active, paused, and queued state, progress, remaining time, source facility, and direct facility navigation.
- Retained facility-scoped production cancellation, reorder, pause, repeat, research cancellation, research pause, prerequisite explanations, affordability state, and command-capacity forecasting.
- Added rally-to-current-view, append-current-view, focus-final-rally, and clear-rally controls through the authoritative production-exit commands.
- Added a research-tree handoff that uses the UI screen API, request API, installed toggle, or `ufr:open-tech-tree` event without taking ownership of the UFR-138 screen.
- Added a bounded deterministic completion ledger with source-facility navigation, restart reset, and explicit-cancellation exclusion.
- Added exact installer teardown restoration, focus visibility, responsive presentation, focused tests, and ownership documentation.

## Parallel isolation

The final comparison against current `main` contains exactly seven UFR-137 files: the economy model, economy installer, panel-specific stylesheet, two focused tests, ownership documentation, and this completion record. It does not modify `src/main.js`, `src/ui.js`, `index.html`, the shared UI skin, command card, minimap, settings, technology-tree implementation, economy simulation, campaign content, art/audio, or sibling games.

The branch was consolidated to a clean linear history after the PR API retained an obsolete divergence-point diff. This removed already-merged work from the PR review surface without changing UFR-137 implementation content. Any later disjoint `main` advancement is incorporated by rebuilding that same seven-file tree on the latest base and rerunning the authoritative workflow.

## Self-review

The complete change was reviewed twice for acceptance coverage, public-command routing, deterministic ordering, immutable normalization, completion false positives, cancellation handling, mission restart behavior, rally coordinate conversion and bounds, facility navigation, research-screen ownership, installer teardown, keyboard focus visibility, responsive layout, branch scope, and parallel ownership.

Corrections made during implementation and review:

- restored the exact prior `ui.refresh` function during disposal;
- preserved the current battlefield center before issuing rally-to-view commands;
- excluded explicit production and research cancellations from the completion ledger;
- reset completion history on mission change or simulation-time rewind;
- added an exact regression for the merged UFR-138 `#techTreeToggle` route rather than relying only on an abstract screen API;
- repeatedly synchronized with current `main`, then consolidated the branch so the final PR diff contains only UFR-137 files.

No PR comments, submitted reviews, unresolved review threads, scope violations, or blocking defects remained after the second review.

## Verification

Focused commands:

```text
node --check src/core/economy-hud-model.js
node --check src/ui/economy-hud-overview.js
node --check tests/ui/economy-hud-model.test.mjs
node --check tests/ui/economy-hud-overview.test.mjs
node --test tests/ui/economy-hud-model.test.mjs tests/ui/economy-hud-overview.test.mjs
```

Implementation blobs:

- `753f5b6d26f7bbc20623650e0193084f77ae8711` — `src/core/economy-hud-model.js`
- `18669aaaf606f10a47836675ac97d1214cc9d772` — `src/ui/economy-hud-overview.js`
- `194bc925c8514aec67cca3442326cd95e02021ef` — `economy-hud.css`
- `870b733cfe40687d1c827d83c7fa814fe566b1fa` — `tests/ui/economy-hud-model.test.mjs`
- `5308c0e9b51c1695fb841494245df3806c73e9f4` — `tests/ui/economy-hud-overview.test.mjs`
- `03f7be1e81d0446636aed5f59e716cffccc09544` — `docs/ECONOMY_PRODUCTION_UI.md`

Authoritative synchronized runs passed on successive current-main assemblies:

- `30977625122` — UFR-138 assembled;
- `30977703083` — completion-marker assembly;
- `30977839566` — UFR-136 plus UFR-138 assembled;
- `30978023525` — clean linear seven-file PR assembly.

Each run passed:

- full assembled `bash verify.sh`;
- active-claim diagnostics;
- browser startup and first-mission smoke;
- completion-evidence audit;
- diagnostics upload.

The final zero-behind head is required to pass the same workflow before merge.

## Runtime composition evidence

The application installs `installEconomyHudOverview()` after the production, research, worker, resource-income, production-exit, and command-capacity controllers. The focused regression verifies that the economy panel activates the merged UFR-138 screen through its actual installed toggle. Browser smoke proves the assembled application starts its first mission with the surrounding command-card, minimap, and technology-tree components composed together.

## Evidence limits

The completion ledger is transient presentation state and introduces no save, replay, checkpoint, profile, or migration schema. No AI behavior changes. Manual all-mission/player walkthroughs and release-gate accessibility/performance checks were not performed, so `PLAYER_VERIFIED` and `RELEASE_VERIFIED` are not claimed.
