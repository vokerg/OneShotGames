# UFR-129 — Add adaptive music

- Final base commit: `fb13719324957b013ba210d97c9df46df802af59`
- Branch: `ufrts/UFR-129-adaptive-music`
- Pull request: #136
- Priority/lane: P1 audio
- Parallel rule: `LIMITED — music system/assets.`
- Dependencies verified on `main`: UFR-124, UFR-125, and UFR-089
- Evidence level: `CONTRACT_COMPLETE`

## Acceptance delivered

- Eight original score states cover menu, briefing, calm, tension, battle, crisis, victory, and defeat.
- Repository-owned deterministic synthesis uses no recordings, sample libraries, model output, network access, or third-party packages.
- Each state produces a four-second mono 16-bit PCM loop at 12 kHz with a 0.72 peak ceiling and canonical SHA-256 evidence.
- The versioned manifest records PCM metadata, gain, loop policy, exact event IDs, and CC0-1.0 provenance.
- Exact UFR-125 `music.state` maps route through the UFR-124 `music` bus without moving campaign or gameplay authority into audio.
- The director implements deterministic stage precedence, intensity thresholds, downward hysteresis, fixed-tick dwell, and immediate mission-outcome transitions.
- The UFR-089 `withdrawal` outcome is accepted and uses the restrained defeat score.
- Stable-state reconciliation starts initial menu music and recovers an externally stopped tagged loop.
- Runtime behavior includes synthesis preload/decode, transient retry, tagged replacement, pause/resume delegation, disposal, and fail-closed error containment.
- Review-loop tooling and `scripts/verify-adaptive-music.mjs` are registered in the authoritative assembled verifier.

## Self-review corrections

1. Battlefield entry bypasses ordinary dwell so the first battlefield score starts immediately.
2. Runtime state checks recover when the active mixer voice disappears externally.
3. Catalog validation enforces peak, sample-count, and PCM byte-length consistency.
4. Stable director updates reconcile playback instead of silently assuming the current state is audible.
5. The valid campaign `withdrawal` outcome maps immediately to the restrained defeat score.

## Verification

Corrected implementation run `30880894313` on head `24f5b9b3f2b0a6d4fb056792636100d0e6dd8b60` passed:

- the full assembled verifier, including deterministic adaptive-music verification;
- active-claim diagnostics;
- browser startup and first-mission smoke;
- completion-evidence audit;
- diagnostics upload.

Earlier complete implementation runs `30880429385` and `30880702956` also passed while the self-review corrections were being layered.

A final marker-inclusive workflow is required on this completion commit before merge.

## Evidence limits

The score recipes, catalog, policy maps, director, mixer runtime boundary, tooling, tests, and documentation are complete. The assembled application does not yet construct the adaptive-music lifecycle, derive a live intensity signal, or audibly review score transitions in a browser. This completion does not claim `RUNTIME_INTEGRATED` or `PLAYER_VERIFIED`.

## Scope guard

No combat or UI SFX, ambience, voice, settings UI, gameplay balance, mission rules, campaign progression, UI layout, application composition, or sibling-game content changed.
