# UFR-140 — Add onboarding help and control reference

- Pull request: #178
- Branch: `ufrts/UFR-140-onboarding-help`
- Evidence level: `CONTRACT_COMPLETE`
- Verification follow-up: #181
- Merge waiver: maintainer instruction on 2026-08-06 to review, repair, and squash-merge the three active PRs despite the inactive Actions runner

## Delivered

- Added a searchable field manual containing tutorial guidance, a glossary, and a control reference.
- Added first-time and contextual hints with persisted seen/dismissed state, reset, dismiss-one, and dismiss-all behavior.
- Added F1 and top-bar access, dialog semantics, focus recovery, status announcements, and exact listener/DOM/global teardown.
- Read tutorial and input-binding contracts without changing simulation state or maintaining a second key map.
- Repaired the reviewed implementation so control entries resolve live bindings after post-install rebinding, specific guide links escape stale category filters, malformed persisted values are normalized, and pending hint timers are cancelled safely during teardown.

## Verification evidence

Focused tests cover catalog composition, live and unbound controls, post-install rebinding, multi-token search, category filtering, persistence, reset/dismiss flows, malformed stored data, first-hint scheduling, timer cancellation, F1/context events, the global API, and repeated disposal.

The required repository workflow did not execute because GitHub Actions remained queued without an active runner. No CI or browser pass is claimed. Issue #181 owns authoritative `bash verify.sh`, browser smoke, and player-flow validation on merged `main`.

## Runtime composition evidence

The onboarding bootstrap is imported by the UI viewport bootstrap and uses the existing safe browser-storage adapter. It reads tutorial and runtime binding contracts and does not wrap `Game`, issue commands, or create simulation phases.

## Browser/player evidence

Browser and manual player verification remain outstanding under #181. In particular, top-bar layout, supported viewport behavior, focus movement, live rebound-key display, contextual hint placement, and real teardown require an active browser run.

## Save implications

No save, campaign, replay, or deterministic simulation schema changes are introduced. The feature stores only presentation-level seen and dismissed hint IDs under a versioned application-local key.

## AI-system use

The feature does not read or modify AI state, doctrine, cadence, observations, commands, or ownership.

## Limitations

- Help copy remains an English presentation contract until it is routed through the localization catalog.
- Completion evidence must not be raised above `CONTRACT_COMPLETE` until #181 records passing integrated and browser checks.
