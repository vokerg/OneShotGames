# BUG-247 — Visual regression browser capture intermittently fails under CI load

- Base: a95854b34fe0bd51d618b0131666e2c003740ac9
- Claimed by: ChatGPT
- Intended files: `scripts/visual-regression-smoke.mjs`, focused tooling tests/fixtures, verification diagnostics under `artifacts/`
- Dependencies verified: recovery issues #109, #110, #111 are closed; no open PR claims BUG-247

## Plan

1. Characterize the current browser timeout/capture lifecycle and identify why normal CI variance can consume the full per-review timeout.
2. Make capture timeout handling deterministic and bounded while persisting actionable failure diagnostics without weakening visual assertions.
3. Add focused regression/stress coverage and run the repository verification set relevant to visual regression tooling.
