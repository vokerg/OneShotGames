# UFR-005 — Add dependency-free unit-test conventions

- Base: c60b82fa00a13b952be7e092669e404c63a634ad
- Claimed by: OpenAI agent via GitHub connector
- Intended files: `tests/`, `scripts/run-tests.mjs`, focused pure helpers in `src/core/` or `src/systems/`, `verify.sh`, and testing documentation
- Dependencies verified: UFR-003

## Plan

1. Add a dependency-free test runner with deterministic discovery, isolated test registration, actionable failure output, and non-zero exit behavior.
2. Add first unit suites for pure math, affordability/cost mutation, upgrade prerequisites, and mission objective evaluation; extract small pure policies where current methods are not directly testable without constructing the browser-facing `Game` facade.
3. Wire the runner into `bash verify.sh`, document conventions and ownership, run the full available verification set, and replace this claim with completion evidence.
