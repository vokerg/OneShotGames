# BUG-273 — Keep operation-selector utility controls reachable

- Issue: #273
- Base: ef7debc1618678f7cd37e9ccd9d7cdf0dde864cf
- Claimed by: ChatGPT
- Intended files: `styles.css`, `scripts/browser-startup-smoke.mjs`, `scripts/browser-localization-smoke.mjs`
- Dependencies verified: current `main`; BUG-270 remains active in PR #286 with non-overlapping intended files

## Plan

1. Make selector state expose only valid top-bar utilities above the operation-selector layer while keeping battlefield-only controls unavailable.
2. Extend the existing browser startup smoke with real hit-target plus pointer/keyboard activation checks before and after starting an operation.
3. Run available CI/browser verification, record any unavailable checks precisely, and keep the PR draft until required evidence is green.
