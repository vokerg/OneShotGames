# BUG-242 Claim

- Agent: ChatGPT
- Claimed: 2026-08-15
- Branch: `ufrts/bug-242-operation-parchment`
- Base-Ref: `f9d54d282f16beb53348a8f2acf10aaa2fd45820`
- Parallel: YES
- Intended files: `ukrainian-front-rts/operation-cards.css`, `ukrainian-front-rts/tests/ui/operation-selector-material.test.mjs`, `ukrainian-front-rts/scripts/visual-regression-smoke.mjs`
- Overlap check: Draft PR #254 (`UFR-160`) was rechecked after browser-capture scope was added and does not modify any intended file; its release-gate ownership is separate from this operation-selector readability defect.
- Task source: GitHub issue #242 — `[Bug][P1][UI] Operation selector parchment frame makes header/footer text unreadable`.
- Queue note: The permanent queue has UFR-160 already claimed with `Parallel: NO`, while UFR-161 remains blocked pending explicit multiplayer approval. This player-visible P1 defect is selected as file-disjoint parallel work per the caller's bug priority.
- Summary: Restore readable operation-selector header/footer copy by removing only the dark inherited border-image center fill, preserving the parchment border and the mission-card reading surface, with focused contract and real-application browser regression coverage.

## Plan

1. Add an operation-selector-specific override that retains the existing nine-slice parchment border while exposing the `.book` light background surface.
2. Add a focused contract test for stylesheet composition, selector scoping, text/background contrast, and mission-card accessibility/readability safeguards.
3. Extend the existing CI visual-smoke path with real-application operation-selector captures at 1920×1080 and the 760px responsive breakpoint.
4. Run the focused test and `bash verify.sh`; if native checkout execution remains unavailable, use the canonical GitHub Actions run for the exact PR head and keep the PR draft until required review evidence is recorded.
