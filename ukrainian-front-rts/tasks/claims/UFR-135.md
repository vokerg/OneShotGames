# UFR-135 — Add production command card

- Base: `860a18ba5fbbedd27cea746c0f4efc60a8326500`
- Claimed by: ChatGPT via GitHub connector
- Intended files: `src/ui/command-card.js`, top-level `src/ui.js` integration, `ui-skin.css`, focused UI tests/verifier, documentation, and this task's records only
- Dependencies verified: UFR-013, UFR-041, UFR-133
- Parallel rule: LIMITED — command-card hotspot
- Active coordination: PR #152 owns audio/settings UI, `src/main.js`, `index.html`, and settings-focused modules/styles; this task will not touch those surfaces

## Plan

1. Extract a deterministic command-card presentation model covering grid slots, pages, action groups, hotkeys, disabled reasons, targeting state, stance state, and stable keyboard navigation.
2. Integrate the model through the existing top-level UI owner without changing command or simulation semantics, and add production-skin presentation within the existing command-card CSS surface.
3. Add focused model, DOM, keyboard, disabled-state, targeting/stance, and composition tests plus authoritative verification and ownership documentation.
4. Run exact-head CI and browser smoke, self-review the complete diff, then replace this claim with completion evidence at the highest justified level.
