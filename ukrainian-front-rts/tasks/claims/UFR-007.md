# UFR-007 — Document and enforce fixed-step simulation phases

- Base: `60a5442245ebb2d803215d60a3d4f76ac2969d44`
- Claimed by: ChatGPT (recovered-task owner)
- Intended files: `src/game.js`, focused modules under `src/systems/` and/or `src/core/`, simulation tests, architecture documentation
- Dependencies verified: UFR-004, UFR-006

## Plan

1. Audit the current frame/update path and define an explicit ordered fixed-step phase contract.
2. Refactor `Game` update delegation so phase order and time-step semantics are deterministic and testable.
3. Add headless scenario coverage proving equivalent outcomes across different render-frame chunking.
4. Run available verification, update architecture documentation, and record completion evidence before review.
