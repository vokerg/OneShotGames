# UFR-133 — Define UI architecture

- Base: `1fb660c273acc3011673d6ed119762e34836b6ff`
- Claimed by: ChatGPT via GitHub connector
- Intended files: new pure modules under `src/ui/`, focused unit tests, and UI architecture documentation
- Dependencies verified: UFR-010, UFR-013
- Parallel isolation: avoid `src/ui.js` and active UFR-058 production command-card work; define the semantic UI contract in new files.

## Plan

1. Define screen stack, modal capture, HUD regions, focus restoration, refresh invalidation, and semantic UI state as browser-independent contracts.
2. Add deterministic tests for stack transitions, modal rules, focus behavior, and refresh batching.
3. Document component ownership and extension rules, run focused verification, synchronize with `main`, and replace the claim with completion evidence.
