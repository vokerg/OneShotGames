# BUG-244 Claim

- Agent: ChatGPT
- Claimed: 2026-08-16
- Branch: `ufrts/BUG-244-command-card-readability`
- Base-Ref: `f9d54d282f16beb53348a8f2acf10aaa2fd45820`
- Parallel: YES
- Intended files: `ukrainian-front-rts/command-card.css`, `ukrainian-front-rts/src/ui/command-card.js`, `ukrainian-front-rts/tests/ui/command-card-readability.test.mjs`, `ukrainian-front-rts/scripts/command-card-readability-smoke.mjs`
- Overlap check: Draft PR #254 changes the release gate and `src/ui/tactical-command-card.js` but not the command-card owner files above; draft PR #255 owns operation-selector styling and `scripts/visual-regression-smoke.mjs`, which this claim deliberately avoids by using a dedicated real-HUD smoke.
- Task source: GitHub issue #244 — `[Bug][P1][UI] Command card clips production names and overlaps repair text`.
- Queue note: BUG-242 is already claimed; BUG-243 naturally overlaps PR #255's `operation-cards.css`/visual-smoke ownership. BUG-244 is the first unclaimed P1 bug that can be completed with file-disjoint ownership while UFR-160 continues in parallel.
- Summary: Make real command-card labels non-overlapping and unambiguous without changing gameplay authority, with full accessible/hover detail for compact labels and a dedicated real-HUD browser regression.

## Plan

1. Reproduce the geometry contract in a focused UI regression test covering long production/repair and localized strings.
2. Fix only command-card presentation/controller metadata so title, description, hotkey/meta, and disabled reason occupy deliberate non-overlapping regions and compact labels expose full meaning.
3. Add a dedicated actual-application HUD smoke/capture for representative production and repair cards, avoiding the active visual-regression script owned by BUG-242.
4. Run focused verification and canonical `bash verify.sh` through the exact-head GitHub Actions path; keep the PR draft and unmerged until all available evidence is recorded.
