# BUG-272 — Remove mixed English/Ukrainian strings from the live Ukrainian locale

- Base: 26f98d6d14807f0273ed14c4e6a313d475207120
- Issue: #272
- Intended files: localization catalogs/adapters, runtime-rendered string owners, localization browser smoke; avoid onboarding-specific ownership in BUG-271 and compact viewport/layout CSS in BUG-270
- Parallel safety: active PR #289 owns onboarding/tutorial runtime behavior and PR #286 owns compact viewport/layout CSS; this branch will not change those mechanics/layout rules unless an unavoidable localization hook is required and documented first.

## Plan

1. Inventory player-facing strings that remain hardcoded or bypass the locale service across the operation selector, active mission HUD/objectives, command card, minimap alerts, notifications, pause/settings surfaces, and authored campaign presentation.
2. Route those strings through the existing localization contract and ensure locale-change events re-render every affected live surface without stale English text.
3. Extend runtime/browser localization coverage for `en → uk → en` on the selector and during an active mission, then run the targeted suite and full verifier available on the branch.
